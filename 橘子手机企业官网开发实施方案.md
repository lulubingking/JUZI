# 橘子手机（JUZI PHONE）企业官网项目开发实施方案

| 文档属性 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 编写日期 | 2026-08-26 |
| 文档状态 | 待确认（用户回复"已确认，执行下一步"后按本方案启动开发） |
| 执行目标 | 本地可运行、可演示的完整系统（前台官网 + 后台管理 + 示例数据 + 自动化测试） |

---

## 1. 执行目标

依据既有文档基线，开发橘子手机企业官网（**前台官网系统 + 后台管理系统**）的完整可运行版本：

| 目标 | 说明 |
|---|---|
| 前台官网 | 10 类页面全部落地：首页 / 产品中心 / 产品详情 / 案例 / 新闻 / 招聘（社招+校招）/ 关于系列 / 联系我们 / 服务中心体系 / 404与维护页 |
| 后台管理 | 17 类视图全部落地：登录 / 工作台 / 产品（列表+编辑+系列）/ 内容（案例、轮播、新闻、页面、FAQ、政策）/ 留言 / 用户权限 / 日志 / 配置 / 统计 |
| 双语 | 界面文案 i18next 切换 + 业务内容双语字段回退（缺英文回退中文） |
| 响应式 | PC / 平板 / 手机三档适配，移动端抽屉导航 |
| 数据库 | MySQL 8.0，21 张表，Alembic 迁移，seed 示例数据一键灌入 |
| 测试 | 后端 pytest（接口契约 + RBAC + 业务规则）、前端关键组件测试 |

**交付目标形态（已确认）**：本地开发 + 演示。生产部署（Nginx/HTTPS）输出配置模板与说明文档，不在本期落地。

---

## 2. 基线文档与资源

| 类别 | 资源 | 作用 |
|---|---|---|
| 需求基线 | 《橘子手机企业官网PRD.md》v1.3 | 功能定义（F-*/B-*）、信息架构、接口清单、里程碑、示例数据 |
| 视觉基线 | 《橘子手机企业官网UIUX设计文档.md》v1.1 | 设计 token、组件规范、页面规范、交互/无障碍/验收标准 |
| 技术基线 | 《橘子手机企业官网开发技术文档.md》v1.0 | 架构、工程结构、接口契约、核心实现方案、任务拆解（**唯一技术实现基线**） |
| 数据基线 | 《橘子手机企业官网数据库设计文档.md》v1.1 + `schema.sql` | 21 张表结构、索引、DDL（**数据库实现以本文档为准**） |
| 前台原型 | `index.html`（10 视图高保真） | 页面效果与视觉实现参考 |
| 后台原型 | `admin.html`（17 视图高保真） | 后台页面结构、AntD 组件映射参考 |
| 设计素材 | `手机素材/`（logo.png + 机型颜色图 + 透明轮播图，共 14 张） | seed 数据与演示图片 |

**冲突处理规则**（沿用各文档约定）：需求冲突以 PRD 为准；视觉交互以 UIUX 为准；数据库字段/DDL 以数据库设计文档为准；技术实现以开发技术文档为唯一基线。

---

## 3. 已确认决策

### 3.1 本次确认（2026-08-26 已与用户逐项确认）

| 决策点 | 结论 |
|---|---|
| 开发环境数据库 | **本机安装 MySQL 8.0**（严格对齐数据库设计文档基线），由开发方负责安装配置 |
| 产品详情图集放大预览（F-DET-01） | **实现灯箱放大**（主图点击弹出灯箱、支持关闭；原型未落地，按开发技术文档 §3.7.1 建议落地） |
| 交付目标 | **本地开发 + 演示**；部署章节输出 Nginx 配置模板与说明，本期不落地生产环境 |

### 3.2 文档已确认决策（直接沿用，不再重复确认）

| 项 | 决策 |
|---|---|
| 技术栈 | 前端 React 18 + TypeScript + Vite（Monorepo 双应用）；后端 FastAPI + SQLAlchemy 2 + MySQL 8.0 |
| 工程形态 | Monorepo：`apps/web`（前台，vite-ssg）+ `apps/admin`（后台，AntD 5）+ `packages/shared`（共享类型）+ `server`（FastAPI）；pnpm workspace |
| SEO | vite-ssg 构建期静态预渲染关键页面（M6 落地） |
| 富文本 | wangEditor 5 + 服务端 bleach 白名单清洗 |
| 图表 | ECharts 5 |
| 认证 | JWT（access 2h + refresh 7d，refresh jti 轮换防重放），BCrypt 密码，5 次失败锁定 30 分钟 |
| 权限 | RBAC 权限点（附录 B 全量），默认三角色：超级管理员 / 内容运营 / 客服 |
| 多语言 | i18next + 双语字段 `xxx_zh/xxx_en`，服务端 `en \|\| zh` 回退 |
| 首页数据 | 前端并行调用公开接口组装（**无独立首页聚合接口**） |
| 产品详情 | 双语完整字段返回（开发技术文档 §6.5 例外约定），前端按语言取用 |
| 搜索 | MySQL LIKE 方案，不建全文索引（数据量 <10⁴ 条） |
| 物理外键 | 默认不建，模型层逻辑引用 + 应用层保证完整性 |

---

## 4. 系统架构与工程形态

### 4.1 总体架构

```
公众访客浏览器 ──► apps/web（React 18 SPA，vite-ssg）──┐
                                                        ├──► FastAPI ──► MySQL 8.0 (juzi_phone)
运营人员浏览器 ──► apps/admin（React 18 + AntD 5）────────┘      │
                                                               └──► uploads/（本地图片存储，预留 OSS）
```

- 接口双域分离：`/api/public/*`（免鉴权，公开）与 `/api/admin/*`（JWT + RBAC）。
- 图片统一经 `POST /api/admin/upload` 上传，返回可访问 URL 入库；开发期 FastAPI 挂载 `/uploads` 静态目录。
- 站点配置存 `site_configs` 表（JSON 分组），维护开关开启时公开接口返回 HTTP 503 + `code=50300`，前台统一渲染维护页。

### 4.2 工程目录结构

```
橘子手机/
├── apps/
│   ├── web/                      # 前台官网（Vite + vite-ssg）
│   │   └── src/
│   │       ├── components/       # 自研组件（layout/ui/product/case/news/home/service/common）
│   │       ├── pages/            # 10 类页面视图
│   │       ├── stores/           # Zustand（locale/search/productFilter）
│   │       ├── i18n/             # locales/zh-CN.json、en-US.json
│   │       ├── services/         # Axios 实例 + api 方法
│   │       ├── hooks/ utils/ styles/（tokens.css）
│   └── admin/                    # 后台管理（Vite + AntD 5）
│       └── src/                  # pages 按 17 类视图；stores（auth/config）
├── packages/
│   └── shared/                   # 共享 TS 类型（DTO、常量、权限点枚举）
├── pnpm-workspace.yaml
├── server/                       # FastAPI 后端
│   ├── app/
│   │   ├── main.py               # 实例、中间件装配、路由注册
│   │   ├── core/                 # config.py / security.py / deps.py
│   │   ├── models/               # SQLAlchemy 模型（21 张表）
│   │   ├── schemas/              # Pydantic v2 DTO（public/admin/common）
│   │   ├── routers/              # public/（11 个）+ admin/（18 个）
│   │   ├── services/             # 业务逻辑层
│   │   ├── middleware/           # request_log / maintenance / rate_limit
│   │   └── utils/                # xss / upload / pagination / i18n
│   ├── alembic/                  # 迁移脚本
│   ├── tests/                    # pytest
│   ├── scripts/                  # seed.py / gen_sitemap.py
│   ├── requirements.txt / .env.example
├── docs/                         # 4 份基线文档（或保持在根目录）
├── 手机素材/                     # 设计素材
└── README.md                     # 本地运行指南（M6 产出）
```

---

## 5. 数据库实施策略

| 项 | 方案 |
|---|---|
| 数据库 | MySQL 8.0（本机安装），库名 `juzi_phone`（数据库设计文档 §7.1），字符集 `utf8mb4` / `utf8mb4_unicode_ci` |
| 建表 | 以 `schema.sql`（已存在，21 张表全量 DDL）为基线；开发期通过 Alembic baseline 迁移建立版本管理 |
| 迁移 | Alembic：`alembic init` → baseline 迁移（对应 schema.sql）→ 后续变更走 `revision --autogenerate`；一次变更一个迁移文件 |
| 模型 | SQLAlchemy 2.x 声明式，21 张表与数据库设计文档 §5 逐字段对齐；模型层声明逻辑外键，物理外键不建 |
| 唯一冲突 | `slug`/`username` 等唯一字段逻辑删除时追加 `-del-<id>` 后缀 |
| 子资源 | 版本/图集/参数/亮点/渠道随产品主记录同事务维护，删除产品级联物理删除子资源行 |
| seed | `scripts/seed.py` 一键灌入 PRD §12 示例数据（4 产品 + 3 案例 + 3 新闻 + 3 轮播 + 4 FAQ + 3 政策 + 6 页面 + 站点配置 + 3 后台账号），图片引用 `手机素材/`，支持清空重置 |
| 保留策略 | 操作日志 90 天、访问统计 180 天（M5 落地清理任务） |

**数据库账号**：创建专用账号（如 `orange`/`orange`）仅授权 `juzi_phone` 库，与开发技术文档附录 C 对齐（库名修正为 `juzi_phone`，见 §11 差异清单）。

---

## 6. 接口实施范围

### 6.1 公开接口（/api/public/*，免鉴权，支持 ?lang）

| 分组 | 接口 |
|---|---|
| 首页/系列 | GET `/banners`、GET `/series` |
| 产品 | GET `/products`（series/sort/featured/page）、GET `/products/{slug}`（双语完整）、GET `/products/{slug}/related` |
| 案例 | GET `/cases`（category）、GET `/cases/{id}`、GET `/cases/{id}/related` |
| 新闻 | GET `/news`（category）、GET `/news/{id}` |
| 内容 | GET `/pages/{page_key}`、GET `/faqs`、GET `/policies` |
| 配置/搜索 | GET `/site-configs`（脱敏）、GET `/search`（keyword/type/limit） |
| 提交 | POST `/messages`（Honeypot + 限流 3 次/分） |
| 统计 | POST `/stats/pv`（埋点，开发技术文档 §7.18 补充接口） |
| SEO | GET `/sitemap.xml`（脚本生成，M6） |

### 6.2 管理接口（/api/admin/*，JWT + RBAC）

| 分组 | 权限点 | 接口范围 |
|---|---|---|
| 认证 | 无/登录态 | login / refresh / me / password |
| 工作台 | stat:view | dashboard/overview、dashboard/trend |
| 产品 | product:* | 产品 CRUD + status/featured/batch-status + 5 子资源（variants/images/specs/highlights/channels 同构模板） |
| 系列 | product:* | series CRUD（删除保护 40900） |
| 案例/轮播/新闻 | cms:* | 各自 CRUD + publish/unpublish/featured/status |
| 页面/FAQ/政策 | cms:* | pages upsert；faqs/policies CRUD |
| 留言 | message:* | 列表（脱敏）/详情/回复/状态流转/删除 |
| 用户/角色 | user:* / role:* | 账号 CRUD + 禁用 + 重置密码；角色权限树；permissions 清单 |
| 日志/统计 | log:view / stat:view | logs 只读；stats overview/visits/pages |
| 配置 | config:* | configs 全量读取 + 分组保存 |
| 上传 | 任意登录用户 | multipart 图片（jpg/png/webp ≤5MB + 魔术字节校验） |

**统一契约**：响应 `{code, data, message}`；分页 `{items, total, page, page_size}`；错误码按附录 A（40100/40110/40300/40400/40900/42200/42900/50300 等）；时间 ISO 8601；金额 JSON 数字。

---

## 7. 里程碑规划（M0 ~ M6）

> 对齐 PRD §11 与开发技术文档 §21 的 M1~M6 划分（验收标准沿用），新增 M0 环境准备。每里程碑结束**交付可运行增量 + 验收说明**，经用户确认后进入下一里程碑。各里程碑内部任务按"后端 → 前端 → 联调"顺序执行。

### M0 环境准备（预计 0.5~1 天）

| 任务 | 说明 |
|---|---|
| 安装 MySQL 8.0 | 本机安装（安装器/winget），配置 root 密码、启动服务；创建 `juzi_phone` 库与专用账号 |
| 前端工具链 | corepack 启用 pnpm（Node 22 自带）；确认 pnpm 8+ 可用 |
| Python 环境 | 创建 `server/.venv`，安装 FastAPI/SQLAlchemy/pydantic-settings/passlib/jose/bleach 等依赖 |
| Monorepo 骨架 | 初始化 apps/web、apps/admin、packages/shared、server 目录与 pnpm-workspace；初始化 git（main/develop 分支） |
| 数据库基线 | 导入 `schema.sql` 建库建表 → Alembic baseline 固化 → 冒烟验证 21 张表齐全 |

**验收**：MySQL 服务正常、`juzi_phone` 库 21 张表可查；`pnpm -v` 可用；后端可连库；git 仓库就绪。

### M1 基础工程（登录认证 + 后台骨架）

| 端 | 任务 |
|---|---|
| 后端 | FastAPI 工程脚手架（main/core/models/schemas 分层）；Alembic 迁移就绪；admin_users/roles/permissions/role_permissions 模型与 seed 账号；JWT 登录/刷新（jti 轮换）、BCrypt、失败锁定；`get_current_user` + `require_permission` RBAC 依赖；上传接口（multipart + 魔术字节校验）；CORS / 请求日志 / 限流中间件；统一响应与异常处理 |
| 前端 | web/admin 双 app 脚手架（Vite + TS strict + 路由 + 布局）；后台登录页（双栏品牌区 + 表单 + 错误条）；后台布局框架（深色侧栏 + 顶栏 + 面包屑）；Axios 实例与拦截器（401 刷新单飞 / 403 提示）；上传组件（Upload.Dragger） |
| 联调 | 登录 → 刷新 → me；无权限接口返回 403；上传图片返回 URL |

**验收（对齐技术文档 §21.1）**：可登录后台、可上传图片、权限骨架生效（无权限接口 403）。

### M2 产品模块

| 端 | 任务 |
|---|---|
| 后端 | products + product_series + 5 子资源表模型；产品 CRUD（保存草稿=下架、起售价自动取最低版本价、子资源同事务全量覆盖）；子资源增量接口（同构模板）；系列 CRUD（删除保护）；公开产品列表（series/sort/featured 过滤）、详情（双语完整）、相关产品接口 |
| 前端 | 后台产品列表（筛选/搜索/上下架/批量操作）、产品编辑页（6 Tab：基础信息/版本/图集/参数/亮点/渠道，双语字段并排，保存草稿/保存并上架）、系列管理页；前台产品中心（系列 Tab + 排序 + 分页 + URL 联动 + 空态）、产品详情页（图集 + **灯箱放大** + 版本联动价格 + 规格表 + 亮点 + 渠道新窗口 + 相关产品） |
| 联调 | 后台完整维护一款产品 → 前台可见可浏览；价格联动与起售价一致；slug 唯一冲突 40900 |

**验收**：后台可完整维护一款产品；前台可浏览产品详情；版本价格联动正确。

### M3 内容模块

| 端 | 任务 |
|---|---|
| 后端 | cases/banners/news/pages/faqs/service_policies 表模型与 CRUD/发布/下线/精选接口；公开内容接口（banners 有效期过滤、cases/news 仅已发布、pages 按 page_key、faqs/policies 仅上线）；富文本 XSS 清洗（bleach 白名单） |
| 前端 | 后台内容管理 6 模块（案例/轮播/新闻/页面内容/FAQ/售后政策，含富文本编辑器封装、双语双 Tab）；前台首页组装（轮播 + 新品推荐 + 产品专区 + 品牌故事 + 精选案例 + 新闻动态，前端并行调用公开接口） |
| 联调 | 首页各区块数据联动正确；轮播有效期规则生效；下线内容前台立即不可见 |

**验收**：后台内容可维护；首页各区块数据联动正确；轮播有效期规则生效。

### M4 前台完善（全页面 + 双语 + 响应式）

| 端 | 任务 |
|---|---|
| 后端 | 留言提交接口（字段校验 + Honeypot 静默拒绝 + 3 次/分限流 + 记录 source_page/locale）；pages 内容接口联调 |
| 前端 | 案例列表/详情（分类 Tab + 分页）、新闻列表/详情、招聘两页（步骤条 + 投递方式）、关于系列 3 页（统一页面内容模板）、联系我们（双栏 + 留言表单 + 成功态）、服务中心/售后政策（锚点目录）/FAQ（手风琴 + 关键词过滤）；i18next 双语全量；响应式适配（3 断点 + 移动抽屉导航）；搜索面板与结果页（联想 ≤8 条） |
| 联调 | 留言提交 → 后台可见；双语切换即时生效；375/768/1024/1440px 无横向滚动 |

**验收**：前台全页面可用、双语切换生效、移动端适配达标。

### M5 系统能力（留言闭环 + 权限 + 统计）

| 端 | 任务 |
|---|---|
| 后端 | 留言管理闭环（列表脱敏/详情/回复/状态机流转/删除）；用户账号 CRUD + 禁用即时生效 + 重置密码（保护规则）；角色管理 + 权限树；操作日志埋点与查询；访问统计聚合接口（PV/UV）；站点配置分组读写（含维护开关） |
| 前端 | 留言管理（抽屉详情 + 状态时间线 + 回复表单 + 脱敏展示）；用户管理、角色权限树（左角色右权限组）；操作日志页（只读）；工作台（统计卡 + 趋势图 + 待办 + 快捷入口）；访问统计页（趋势 + Top 榜 + 来源分布，ECharts）；站点配置页（5 分组 Tab + 维护开关） |
| 联调 | 客服处理留言闭环（待处理→处理中→已回复）；三角色权限矩阵生效；统计图表数据正确 |

**验收**：客服处理留言闭环；权限矩阵生效；统计图表正确。

### M6 优化上线（本地演示版）

| 端 | 任务 |
|---|---|
| 后端 | sitemap 生成脚本；安全加固复查（XSS 白名单 / 上传校验 / 响应头）；seed 数据全量灌入与验证；日志/统计清理任务 |
| 前端 | vite-ssg 预渲染接入（关键 7 类页面）+ SEOHead（meta 动态化）；性能优化（路由分包 + 懒加载 + 图片懒加载）；维护页（503 拦截渲染）与 404/内容不存在页完善；README 本地运行指南 + Nginx 配置模板（`/api` 反代 + 双 dist 托管 + /uploads 别名 + 安全头） |
| 联调 | 全量接口冒烟（pytest 接口测试集）；维护开关 → 前台维护页；预渲染产物可访问 |

**验收**：通过 SEO/性能/安全自查清单；演示数据齐备；一键启动本地运行。

---

## 8. 开发规范与质量保障

| 项 | 规范 |
|---|---|
| 代码规范 | TS strict + ESLint + Prettier；Python ruff + 类型标注；Pydantic schema 命名 `XxxCreate/Update/Out`；Conventional Commits |
| 后端测试 | pytest：service 层业务规则（价格联动、状态流转、双语回退）、公开接口契约（分页/筛选/回退）、管理接口鉴权与 RBAC（权限点矩阵）、留言校验与限流；**验收门槛：核心接口与 RBAC 用例通过率 100%** |
| 前端测试 | Vitest + Testing Library：VariantSelector 价格联动、FaqAccordion、留言表单校验、路由守卫 |
| 联调流程 | 契约先行（后端 Swagger /docs）；前端接口未就绪用 shared mock 数据；每模块按开发技术文档 §18.3 联调验收清单核对 |
| 验收标准 | 每里程碑按 §7 对应验收标准核对，交付"可运行增量 + 验收说明" |

---

## 9. 本地环境搭建与运行指南（目标态）

```bash
# 1. 数据库（M0 完成安装后）
mysql -uroot -p -e "CREATE DATABASE juzi_phone DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

# 2. 后端
cd server
python -m venv .venv && source .venv/Scripts/activate    # Windows
pip install -r requirements.txt
cp .env.example .env                                     # 修正 DATABASE_URL 库名为 juzi_phone
alembic upgrade head                                     # 或直接导入 schema.sql
python scripts/seed.py                                   # 灌入示例数据
uvicorn app.main:app --reload --port 8000                # Swagger: http://localhost:8000/docs

# 3. 前端（另开终端）
corepack enable pnpm                                     # 首次
pnpm install
pnpm --filter web dev                                    # 前台 http://localhost:5173
pnpm --filter admin dev                                  # 后台 http://localhost:5174
```

**演示账号**（seed 提供）：`admin`（超级管理员）、`operator`（内容运营）、`support`（客服），初始密码按 PRD §12.4 约定（首次登录强制改密）。

---

## 10. 文档差异处理清单

| # | 差异点 | 处理结论 |
|---|---|---|
| 1 | 数据库名：数据库设计文档 `juzi_phone` vs 开发技术文档 .env.example `orange_phone` | **以数据库设计文档为准**：库名 `juzi_phone`，`.env` 同步修正 |
| 2 | 埋点接口：PRD §8.1 未列 `POST /api/public/stats/pv`，开发技术文档 §7.18 补充 | 按开发技术文档实施（与 PRD B-STAT 需求一致，仅补充传输契约） |
| 3 | 产品详情返回：PRD §8.1 描述按 `?lang` 返回；开发技术文档 §6.5 例外约定"详情双语完整返回" | 按开发技术文档：详情返回 `*_zh/*_en` 全量，前端取用回退 |
| 4 | "每页列表条数"：PRD §5.7 有该配置项，但 site_configs JSON 结构未定义该字段 | 以常量 `PAGE_SIZE=12`（上限 100）实现；配置化留待二期 |
| 5 | 站点配置分组：PRD 提"其他"分组 | 以数据库设计文档为准：5 个固定 key `basic/contact/social/footer/maintenance` |
| 6 | 首页新品推荐数据源：PRD §8.1 产品列表参数未列 `featured`，开发技术文档 §7.3 补充 | 按开发技术文档：`GET /api/public/products?featured=1` 实现 |

---

## 11. 交付物清单

| 阶段 | 交付物 |
|---|---|
| M0 | MySQL 环境、Monorepo 骨架、21 张表基线迁移、git 仓库 |
| M1~M5 | 各里程碑可运行增量（前后端代码）+ 阶段验收说明 |
| M6 | 完整系统 + `README.md`（本地运行指南）+ Nginx 部署配置模板 + seed 示例数据 + 测试报告 |
| 全程 | 后端 pytest 测试集、前端关键组件测试、数据库迁移脚本 |

---

## 12. 风险与依赖

| 项 | 说明 | 应对 |
|---|---|---|
| MySQL 8.0 安装 | 需下载安装包/可能需管理员权限 | M0 优先处理；如安装受阻降级为开发技术文档 §2.4 的 SQLite 兼容模式并通知用户 |
| pnpm 首次启用 | corepack 首次下载 pnpm 需网络 | 如网络受限改用 npm workspaces（工程结构不变） |
| wangEditor 与 React 18 兼容性 | 实施时验证 | 若出现兼容问题备选 quill 封装，保持富文本接口一致 |
| 素材路径 | seed 引用 `手机素材/` 相对路径 | 开发期将素材复制/挂载至 `uploads/`，统一 URL 前缀 `/uploads/` |
| 图片外部链接 | 原型中示例外链（京东/天猫）为占位 | seed 使用占位链接，PRD O-06 法务复核属上线前事项（本期不做） |

---

## 13. 开放问题（沿用 PRD §13.2，本期不阻塞）

O-01 在线客服 IM（不做）、O-02 招聘在线投递（二期）、O-03 留言回复访客可见（二期）、O-04 第三方统计（可叠加）、O-05 部署环境（本期本地演示）、O-06 外链法务复核（上线前）。

---

## 14. 执行流程

1. 用户回复 **"已确认，执行下一步"** → 启动 M0（环境准备：安装 MySQL 8.0、初始化 Monorepo、数据库基线）。
2. 每里程碑完成 → 交付增量与验收说明 → 用户确认后进入下一里程碑。
3. M6 完成 → 交付完整系统、运行指南、测试报告。

*本方案为橘子手机企业官网开发执行基线，变更须经用户确认并更新修订记录。*
