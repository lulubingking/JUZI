# 橘子手机（JUZI PHONE）企业官网 UI/UX 设计文档

| 文档属性 | 内容 |
|---|---|
| 文档版本 | v1.1 |
| 编写日期 | 2026-08-26 |
| 文档状态 | 初稿（依据 PRD v1.3 与高保真原型固化） |
| 关联文档 | 《橘子手机企业官网PRD.md》v1.3（需求基线） |
| 视觉依据 | `index.html`（前台高保真原型，10 视图）、`admin.html`（后台高保真原型，16 视图） |
| 规范框架 | ui-ux-pro-max 设计指南（无障碍 / 触摸交互 / 性能 / 布局响应式 / 字体颜色 / 动画 / 风格 / 图表 8 类规则） |
| 适用范围 | 前台官网系统（React 自研组件）+ 后台管理系统（Ant Design）的前端实现与视觉验收 |

---

## 修订记录

| 版本 | 日期 | 修订人 | 修订说明 |
|---|---|---|---|
| v1.0 | 2026-08-26 | WorkBuddy | 首次定稿：基于 PRD v1.3 与两套高保真原型，输出完整 UI/UX 设计规范（设计系统 / 组件 / 页面 / 交互 / 无障碍 / 验收） |
| v1.1 | 2026-08-26 | WorkBuddy | 专家审核修订：①新增 §9.10 站点维护页（F-GLB-05，含布局/视觉/503 接口行为），404 顺延为 §9.11；②产品详情图集补充"放大预览"标注（PRD F-DET-01 要求，原型未落地，列为待确认项）；③上市日期补充"可空则隐藏"；④§11.6 触控目标规则严谨化（42px 图标按钮间距隔离）；⑤首页模块表补 F-HOME-07 编号与品牌故事指标标签；⑥附录 C 修正 F-GLB 交叉引用 |

---

## 目录

1. [文档说明](#1-文档说明)
2. [设计理念与设计原则](#2-设计理念与设计原则)
3. [设计系统：色彩](#3-设计系统色彩)
4. [设计系统：字体与排版](#4-设计系统字体与排版)
5. [设计系统：布局与栅格](#5-设计系统布局与栅格)
6. [设计系统：圆角、阴影、图标与图片](#6-设计系统圆角阴影图标与图片)
7. [组件规范：前台官网](#7-组件规范前台官网)
8. [组件规范：后台管理](#8-组件规范后台管理)
9. [页面规范：前台](#9-页面规范前台)
10. [页面规范：后台](#10-页面规范后台)
11. [交互与动效规范](#11-交互与动效规范)
12. [无障碍与多语言适配](#12-无障碍与多语言适配)
13. [验收清单与附录](#13-验收清单与附录)

---

## 1. 文档说明

### 1.1 文档定位

本文档是橘子手机企业官网的 **UI/UX 设计规范基线**，向下承接 PRD v1.3 的功能定义，向上固化两套高保真原型（前台 `index.html`、后台 `admin.html`）已落地的视觉语言、组件形态与交互规则。PRD 回答"做什么"，本文档回答"长什么样、怎么交互、按什么标准验收"。

### 1.2 适用范围

| 范围 | 说明 |
|---|---|
| 前台官网 | 首页、产品中心、产品详情、案例、新闻、招聘、关于、联系我们、服务中心体系、404（共 10 类页面） |
| 后台管理 | 登录、工作台、产品/内容/留言/用户权限/系统设置（共 16 类页面） |
| 组件层 | 前台自研组件（React + CSS Modules / styled 体系）、后台 Ant Design 组件映射 |
| 不在范围 | 品牌 VI 手册（logo 规范独立）、暗色模式全站方案（本期不做，见 2.4） |

### 1.3 阅读对象

- **前端开发**：按第 3~10 章落地组件与页面，按第 11~13 章自查交互与验收。
- **UI/UX 设计**：后续迭代所有视觉改动须以本文档 token 与组件规范为准。
- **测试/QA**：以第 13 章验收清单作为视觉与交互验收依据。
- **产品/运营**：了解内容录入侧的形态约束（如图片比例、双语字段布局）。

### 1.4 与 PRD 的关系

- 需求冲突时以 **PRD v1.3** 为准；视觉与交互细节以**原型为准**；本文档负责将两者翻译为可执行的规范。
- 功能编号（F-*/B-*）与本文档章节建立交叉引用（见附录 C），便于追溯。

### 1.5 术语与约定

| 术语 | 含义 |
|---|---|
| token | 设计令牌，指色彩/字体/间距等以变量形式固化的设计值 |
| 前台 / 后台 | 前台官网系统 / 后台管理系统 |
| 状态矩阵 | 组件在各交互状态（default/hover/active/disabled/loading）下的视觉定义 |
| 双语字段 | 以 `xxx_zh` / `xxx_en` 存储的内容字段，缺英文回退中文（PRD §9） |

---

## 2. 设计理念与设计原则

### 2.1 品牌关键词

橘子手机的视觉气质围绕品牌内核"**鲜活科技**"展开，三个关键词：

| 关键词 | 视觉表达 |
|---|---|
| 鲜活 | 主色"橘子橙"#FF6A00；大圆角（14/20px）；暖色渐变光晕；浅橙底点缀 |
| 科技 | 克制的排版、清晰的栅格、线性 SVG 图标、克制的动效（150–300ms） |
| 温暖 | 柔和阴影（低透明度深棕）、暖灰中性色（#F7F5F1 浅底）、深色区用暖棕而非纯黑 |

### 2.2 视觉方向

- **亮色主题**：白底 + 浅灰底交替（`#FFFFFF` / `#F7F5F1`）形成区块节奏，深色仅用于页脚、轮播底、页面 Hero 与后台侧栏等**沉浸区**。
- **图片优先**：产品图/封面图占卡片主体（1:1、16:9），文字信息次级排布，对齐 mi.com 的视觉层级参考。
- **圆润亲和**：按钮全胶囊（999px）、卡片 20px 大圆角，弱化硬朗科技感。

### 2.3 设计原则（5 条）

1. **品牌一致性**：品牌橙贯穿前后台（token 同源），后台在 Ant Design 基座上进行主题定制而非另起风格。
2. **内容优先**：一屏一焦点，产品/案例/新闻卡片信息密度有序（图 → 标题 → 摘要 → 元信息）。
3. **简洁克制**：主色仅用于强调（按钮、选中态、数字、链接），大面积使用中性色。
4. **高效可达**：键盘可完整操作、触控目标 ≥ 44px、状态不以颜色为唯一指示。
5. **双语一体**：中英切换即时生效且不破版，字段级双语布局（中英并排/分 Tab）贯穿前后台。

### 2.4 设计范围声明

- 本期**仅亮色主题**，不提供全站暗色模式；后台深色侧栏、轮播/页脚深色区为**局部深色场景**，不属于暗色模式。
- 后续如需暗色模式，应在设计系统层预留 token 扩展（`--bg-*`、`--ink-*` 语义化），本期不做。

---

## 3. 设计系统：色彩

### 3.1 前台色彩 token（取自 index.html）

| token | 色值 | 用途 |
|---|---|---|
| `--primary` | `#FF6A00` | 品牌主色：按钮、选中态、链接、强调数字 |
| `--primary-strong` | `#E85D00` | 主色悬停、价格数字、深底上的强调 |
| `--primary-soft` | `#FFF4EC` | 浅橙底：标签、价格框、选中底色 |
| `--primary-tint` | `#FFE9D8` | 更浅橙：FAQ 展开边框等装饰 |
| `--ink` | `#1A1A1A` | 主文字（标题、正文） |
| `--ink-2` | `#56534E` | 次级文字（摘要、说明） |
| `--ink-3` | `#9A958C` | 弱化文字（元信息、占位、面包屑） |
| `--line` | `#E9E6E1` | 常规边框、分隔线 |
| `--line-strong` | `#DCD8D0` | 强调边框（输入框、胶囊按钮描边） |
| `--bg` | `#FFFFFF` | 页面主背景 |
| `--bg-soft` | `#F7F5F1` | 浅灰底（交替区块、卡片底色、指标卡） |
| `--dark` | `#1C1A17` | 深色区主底（页脚、Hero、按钮 dark） |
| `--dark-2` | `#26231F` | 深色次级底 |
| `--dark-ink` | `#F2EFE9` | 深色区主文字 |
| `--dark-ink-2` | `#A9A49B` | 深色区次级文字 |

### 3.2 后台色彩 token（取自 admin.html）

| token | 色值 | 用途 |
|---|---|---|
| `--primary` 系列 | 同前台 | 主操作、激活侧栏项、Tab 选中 |
| `--ink` | `#1C1B20` | 主文字 |
| `--ink-2` | `#5A5761` | 次级文字 |
| `--ink-3` | `#9A97A0` | 弱化文字 |
| `--line` | `#E8E7EC` | 表格/卡片边框 |
| `--line-strong` | `#DAD8DF` | 输入框边框 |
| `--bg` | `#F5F6F8` | 内容区背景 |
| `--card` | `#FFFFFF` | 卡片背景 |
| `--sidebar` | `#1E1F26` | 深色侧栏底 |
| `--sidebar-2` | `#26272F` | 侧栏悬停底 |
| `--sidebar-ink` | `#9EA0AB` | 侧栏文字 |
| `--success` | `#22A65E` | 成功/已发布/启用 |
| `--danger` | `#E5484D` | 危险/删除/待处理/错误 |
| `--warn` | `#E8A30C` | 警告 |
| `--info` | `#3B82F6` | 信息/链接类标识 |

### 3.3 语义色与状态标签

后台状态标签（tag）色板及语义（原型已固化的用法）：

| tag 类 | 底色 | 文字色 | 语义示例 |
|---|---|---|---|
| `tag-green` | #E7F6EE | #22A65E | 上架、已发布、启用、已回复、上线 |
| `tag-orange` | #FFF4EC | #E85D00 | 精选、处理中、企业新闻、超级管理员 |
| `tag-gray` | #F0F0F2 | #5A5761 | 停用、禁用、已下线、默认版本 |
| `tag-red` | #FEEBEC | #E5484D | 待处理、删除类警示 |
| `tag-blue` | #EAF2FE | #3B82F6 | 行业资讯、内部页面、内容运营、Flip 系列 |
| `tag-purple` | #F3EFFC | #7C5CE0 | 零售分类、X 系列 |

> 规则：状态展示**必须文字 + 颜色**并存（颜色不作唯一指示，满足无障碍要求）。

### 3.4 对比度要求

| 场景 | 要求 | 说明 |
|---|---|---|
| 正文（ink / ink-2） | 对比度 ≥ 4.5:1 | `#1A1A1A`、`#56534E` 均达标 |
| 弱化文字（ink-3） | 对比度 ≥ 3:1，且不承载关键信息 | `#9A958C` 仅用于元信息/占位 |
| 品牌橙 + 白字 | 实测约 **2.9:1**，不满足正文级 AA | **主色禁止用于正文**；按钮/大号标题使用可接受，悬停须加深至 `#E85D00`（约 3.5:1） |
| 深色区文字 | `--dark-ink`（#F2EFE9）≥ 4.5:1 | 深底浅字方向达标 |

### 3.5 使用规则

- 主色大面积使用比例 ≤ 30%，用于强调而非铺底。
- 前台 hover 统一向 `--primary-strong` 加深；后台 hover 以背景色微变 + 文字变主色表达。
- 选中态 vs 悬停态必须有差异（前台：选中深底白字/主色描边，悬停浅橙底）。

---

## 4. 设计系统：字体与排版

### 4.1 字体栈（前后台统一）

```css
--font: "Inter", "PingFang SC", "HarmonyOS Sans SC", "Microsoft YaHei", "Noto Sans SC", -apple-system, sans-serif;
```

- 西文/数字优先 Inter；中文回退 PingFang SC（macOS/iOS）→ HarmonyOS Sans SC → Microsoft YaHei。
- 禁止引入衬线字体；全站无衬线保证跨平台观感一致。

### 4.2 字号阶

**前台**（基础 16px）：

| 层级 | 字号 | 字重 | 用途 |
|---|---|---|---|
| Hero 标题 | clamp(32px, 4.6vw, 54px) | 800 | 轮播主标题 |
| 区块标题 H2 | clamp(26px, 3vw, 38px) | 800 | section-head 标题 |
| 页内标题 H2/H3 | 22–26px | 800 | 详情页亮点/规格标题 |
| 卡片标题 | 16–17.5px | 700 | 产品/案例/新闻卡片标题 |
| 正文 | 16px | 400 | 默认正文 |
| 正文小号 | 14–15.5px | 400/600 | 表单、列表正文、导航 |
| 辅助文字 | 12.5–13.5px | 500/600 | 元信息、摘要、标签、面包屑 |
| eyebrow | 13px | 700（letter-spacing .16em） | 区块英文小标 |

**后台**（基础 14px，信息密度更高）：

| 层级 | 字号 | 字重 | 用途 |
|---|---|---|---|
| 页面标题 | 20px | 700 | page-head |
| 卡片标题 | 15px | 700 | card-title |
| 表格正文 | 13.5px | 400/600 | table |
| 表单正文 | 13.5px | 400 | input/select |
| 标签 tag | 12px | 600 | tag |
| 辅助 | 11–12.5px | 400 | hint、meta |

### 4.3 行高与字距

- 正文行高 1.65（后台 1.6）；大段说明文字 1.85–1.95。
- 标题行高 1.1–1.25；标题使用 `letter-spacing: -0.01em ~ -0.02em` 收紧。
- eyebrow 大写英文小标使用 0.14–0.16em 字距。

### 4.4 双语排版规则

- 中英字段**同权重**：前台 UI 文案经 i18n 切换；内容字段取对应语言，缺英文回退中文（PRD §9.2）。
- 溢出处理：卡片标题/摘要统一两行截断（`-webkit-line-clamp: 2`）；英文长词允许断行（`word-break: break-word`）或保留 `white-space: nowrap`（按钮、标签）。
- 按钮/标签文案增长时**不换行**，优先允许容器横向扩展；移动端允许缩短文案。
- 中英文混排时西文数字使用 Inter 字体特征（如价格 `¥2,488` 千分位）。

### 4.5 金额样式

- 前台价格：小号 `¥` 符号 + 大号数字（20–32px、800 字重、`--primary-strong` 色），如 `¥2,488`。
- 起售价语义：展示"起售价"标签 + 最低版本价格；选中版本后展示该版本价格（PRD F-DET 版本价格展示规则）。
- 后台表格价格：常规文字 + 千分位。

---

## 5. 设计系统：布局与栅格

### 5.1 容器

- 最大内容宽 **1240px**（`--container`），两侧内边距 24px（移动端 18px），居中对齐。
- 顶部导航、页脚与主体使用同一容器宽度，保证视觉轴线一致。

### 5.2 间距基准（8px 倍率）

常用间距值：**8 / 12 / 14 / 16 / 20 / 22 / 24 / 26 / 28 / 32 / 36 / 40 / 44 / 48 / 56 / 60 / 64 / 88**。

- 区块上下内边距：`section` 88px（移动端 60px）。
- section-head 与内容间距：44px（移动端 30px）。
- 卡片内边距：20–26px（前台）；14–20px（后台）。

### 5.3 响应式断点（对齐 PRD §10.1）

| 断点 | 设备 | 关键行为 |
|---|---|---|
| ≥ 1200px | 桌面 | 产品网格 4 列、案例/新闻 3 列；导航全量 + 二级下拉 |
| 768–1199px | 平板 | 产品 3 列、案例/新闻 2 列；`≤1024px` 时详情/关于/联系等双栏布局收为单栏，轮播产品图隐藏 |
| < 768px | 手机 | 产品 2 列、案例/新闻 1 列；抽屉式导航；表单单列；轮播去箭头保留手势 |

> 后台断点：`≤1200px` 统计卡 5→3 列、登录页双栏收单；`≤900px` 侧栏隐藏；`≤640px` 表单双列收单列。

### 5.4 栅格与列数

| 网格 | 桌面 | 平板 | 手机 |
|---|---|---|---|
| 产品卡（product-grid） | 4 | 3 | 2 |
| 案例卡（case-grid） | 3 | 2 | 1 |
| 新闻卡（news-grid / news-list-grid） | 3 | 2 | 1 |
| 系列卡（series-grid） | 4 | 1（≤1024） | 1 |
| 步骤条（steps） | 社招 4 / 校招 5 | 2 | 1 |

### 5.5 区块节奏

- 页面骨架统一：**吸顶导航 → 内容区（面包屑 → 筛选/主体 → 分页）→ 页脚**。
- 交替背景：内容区块按"白 → 浅灰"交替（`.alt-bg`），首页 6 个模块形成节奏。
- 面包屑：页面顶部 26px 起始，13.5px、`ink-3`，分隔符 `/`。

### 5.6 头部与导航布局

- 头部高度 72px（移动端 62px），`position: sticky; top: 0; z-index: 100`。
- 滚动后背景加深（白 88% → 96%）+ 底部边框 + 阴影（0 6px 24px）。
- 布局：`[Logo 38px] [主导航 5 项] ... [语言切换] [搜索] [汉堡(移动)]`。

---

## 6. 设计系统：圆角、阴影、图标与图片

### 6.1 圆角

| 场景 | 前台 | 后台 |
|---|---|---|
| 组件级 | 14px（`--radius`） | 8–10px |
| 大卡/面板 | 20px（`--radius-lg`） | 12px |
| 胶囊 | 999px（按钮、标签、Tab、颜色选项） | 99px（switch） |
| Logo 圆角 | 11px | 9px |

### 6.2 阴影

前台三级（暖棕低透明度）：

| 级别 | 定义 | 用途 |
|---|---|---|
| `--shadow-sm` | 0 2px 10px rgba(28,26,23,.06) | 卡片默认、表单卡 |
| `--shadow` | 0 10px 34px rgba(28,26,23,.10) | 下拉面板 |
| `--shadow-lg` | 0 22px 60px rgba(28,26,23,.16) | 卡片 hover、抽屉 |

- 卡片 hover 动效统一：`translateY(-6px)` + `shadow-lg` + 边框透明化。
- 按钮 hover 附加主色投影：`0 8px 22px rgba(255,106,0,.32)`。
- 后台阴影克制：`0 2px 10px rgba(28,27,32,.06)` 卡片、`0 14px 40px` 抽屉。

### 6.3 图标规范

- **统一使用线性 SVG 图标**（stroke 1.4–2.4、`currentColor`），禁止 emoji 作图标。
- 建议图标集：Lucide / Heroicons（线性风格与原型一致），后台可沿用 AntD 内置图标 + 线性覆盖。
- 尺寸分级：前台 14–22px（导航 20、按钮 16、状态 14）；后台 14–18px（侧栏 17）。
- 纯图标按钮必须带 `aria-label`（如搜索、汉堡、关闭）。

### 6.4 图片素材规范（对齐 PRD §12.5）

| 场景 | 比例 | 说明 |
|---|---|---|
| 产品卡片图 | 1:1（aspect-ratio 1/1） | 产品图 82% 居中，浅米渐变底 |
| 产品详情主图 | 1:1 | 主图 88–90%，`mix-blend-mode: multiply` 融合浅底 |
| 详情缩略图 | 76×76px | active 主色 2px 描边 |
| 案例封面 | 16:9 | 灰棕渐变占位 |
| 新闻封面 | 16:8.4 | 同上 |
| 首页轮播 | 16:7（后台提示） | 建议上传此比例；轮播右侧透明底产品图（`机型 颜色 透明.png`） |
| 品牌/招聘/服务配图 | 4:3 / 4:4.4 / 16:9 | 深棕渐变视觉区 |

- 素材命名：`机型 颜色.png`（如 `JUZI 1 橘红.png`）、透明底 `机型 颜色 透明.png`、Logo `logo.png`，存放于 `手机素材/`。
- 上传约束（后台）：jpg/png/webp、≤ 5MB、封面建议比例提示；图片懒加载 + WebP 优先（PRD §10.3）。

### 6.5 图片兜底

- 封面缺失时使用**灰棕渐变占位图 + SVG 图标**（原型已实现），禁止出现破图。
- 产品图缺失时展示渐变底占位；alt 文本必填（产品名/机型）。

---

## 7. 组件规范：前台官网

### 7.1 通用规则

- 状态矩阵：每个交互组件定义 `default / hover / active / disabled`（+ loading）四态。
- 所有可点击元素 `cursor: pointer`；过渡 `150–300ms ease`，优先 transform/opacity。
- 禁用态：`opacity: .5 + pointer-events: none`。

### 7.2 按钮（.btn 系列）

| 变体 | 默认 | hover |
|---|---|---|
| `btn-primary` | 橙底白字 | 深橙 `#E85D00` + 上浮 1px + 主色投影 |
| `btn-ghost` | 白底 + 1.5px 描边 | 主色描边 + 主色文字 |
| `btn-dark` | 深底白字 | 纯黑底 |
| 尺寸 | 高 46px（lg 52 / sm 38），圆角 999px，字重 600 | — |

### 7.3 标签（.tag）

- 标准 tag：浅橙底橙字（26px 高胶囊）；`tag-plain` 浅灰底次级字；`tag-dark` 半透明白底（深色区用）。
- 用于系列名、状态、分类；后台 tag 体系见 8.7。

### 7.4 面包屑（.breadcrumb）

- 13.5px、`ink-3`；可点击项 hover 变主色；当前项 `ink-2` + 500 字重；分隔 `/`。

### 7.5 筛选条与 Tab（.filter-bar / .tab）

- 白底卡片式筛选条：圆角 16px、`shadow-sm`，含 Tab 组 + 右侧排序/计数。
- Tab：38px 高胶囊；hover 浅橙底橙字；**active 深底白字**（与后台 Tab 下划线样式区分，保持各自体系一致）。

### 7.6 排序下拉（.sort-select / .sort-menu）

- 触发：描边胶囊 + 箭头；hover 主色描边；展开菜单 180px 白底圆角 12px `shadow`。
- 菜单项 hover/active：浅橙底橙字。

### 7.7 分页（.pagination）

- 居中（前台）居右（后台）；按钮 40×40 圆角 11px 描边；active 橙底白字；禁用 40% 透明。

### 7.8 空态（.empty-state）

- 居中：56px SVG 图标（35% 透明）+ 文案 + 可选操作按钮（如"清除筛选"）。
- 文案须友好且给出下一步（PRD F-LIST-05、F-CASE-04）。

### 7.9 卡片体系

**产品卡（product-card）**：1:1 图区（浅米渐变）→ 名称 17.5px/700 → 卖点 13.5px → 起售价（橙 20px/800）+ 查看详情；hover 上浮 + 大阴影。
**案例卡（case-card）**：16:9 封面 → 标题 16.5px → 摘要两行截断 → 元信息（分类 tag + 日期）。
**新闻卡（news-card）**：16:8.4 封面 → 标题 16px 两行截断 → 摘要两行截断 → 日期。

> 统一 hover：`translateY(-6px)` + `shadow-lg` + 边框透明化，过渡 250ms。

### 7.10 表单体系（联系我们 / 搜索）

- 布局：`form-grid` 两列（移动端单列），`.full` 占整行。
- 控件：1.5px 描边、圆角 12px、内边距 12/16px；**focus 主色描边 + 3px 主色光晕** `rgba(255,106,0,.12)`。
- 错误态：红描边 + 内联 `err-msg`（12px 红字，就近展示）。
- 字数统计：textarea 右下 `0/2000` 计数。
- 必填标记：label 内红色 `*`。
- 隐私勾选：`agree-row` 复选 + 主色链接《隐私声明》+ 未勾选内联错误。
- 提交成功：整卡切换为成功态（64px 绿圆勾 + 标题 + 说明），表单清空（PRD F-CONT-02）。

### 7.11 导航体系

**主导航（PC）**：
- 一级项：15.5px/600，`ink-2`；hover/active 变主色 + 底部 3px 主色下划线（scaleX 动效）。
- 二级下拉：白底圆角 16px、`shadow`、min-width 200px（宽版 232px）；项含 30px 图标底 + 文字，hover 浅橙底左移 2px；支持 focus-within 展开（键盘可达）。
- 关于我们下拉含分组小标题（"品牌与公司" / "沟通与咨询"）。

**语言切换（lang-switch）**：浅灰底胶囊，激活项白底主色字 + `shadow-sm`；中 / EN 两键。

**搜索面板（search-panel）**：头部下方展开；输入框 54px、focus 主色描边；联想列表分组，每条带类型标签（产品/案例/新闻，`s-type` 浅橙底橙字），最多 8 条；空结果展示提示。

**移动端抽屉（drawer）**：320px（max 86vw）右侧滑入 + 半透明遮罩；一级项可展开二级（箭头 90° 旋转）；底部放语言切换与服务中心入口。

### 7.12 轮播（hero-slider）

- 结构：全宽深色渐变底（暖棕 140°）+ 左侧文案（eyebrow 标签 / 54px 标题 / 副文案 / 双 CTA）+ 右侧透明底产品图（drop-shadow 投影）。
- 交互：自动播放 5s/张；指示点（26×5 圆角条，active 38px 橙）；两侧 46px 半透明圆形箭头（hover 橙底）；移动端隐藏箭头保留手势。
- 切换过渡：opacity .7s + visibility。

### 7.13 产品详情组件（F-DET）

- 布局：左图右文（`detail-layout` 1.05fr:1fr），图区 `position: sticky`（跟随滚动）。
- 图集：主图 1:1 浅底 + 缩略图横排（76px、active 主色描边）。放大预览为 PRD F-DET-01 要求，原型未落地，实现时提供点击放大/灯箱（列为待确认项）。
- 价格框：浅橙底圆角 14px，`起售价` 标签 + 32px/800 橙数字。
- 颜色选项：色点（20px 圆 + 描边）+ 名称胶囊；active 主色描边 + 浅橙底。
- 存储选项：规格胶囊（`12GB+256GB` + 小号价格），active 同色规则。
- 渠道按钮：描边胶囊 + 主色图标，hover 主色 + 上浮；点击新窗口打开（PRD F-DET-04）。
- 亮点块（highlight-block）：图文交错，偶数块 RTL 翻转实现左右交替；深灰渐变配图区 16:10。
- 参数表（spec-table）：分组行（浅灰底 th 全宽）+ 参数行（首列 34% 宽、`ink-3` 弱化）；圆角 14px 容器。
- 相关产品：复用 product-grid，最多 4 款（PRD F-DET-07）。

### 7.14 步骤条（.steps）

- 卡片式步骤：38px 橙色序号圆点（浅橙底橙字）→ 标题 → 小字说明。
- 步骤间以虚线连接（repeating-linear-gradient，最后一项去除）；≤1024px 收 2 列断线，移动端 1 列。
- 数量：社招 4 步、校招 5 步（PRD F-CAREER）。

### 7.15 时间线（.timeline）

- 左侧主色竖线（渐变至灰）+ 白心橙边圆点；年份 `primary-strong` 800 字重 + 事件标题 + 说明。

### 7.16 FAQ 手风琴（.faq-item）

- 折叠项：白底描边圆角 14px；问题行 15.5px/700（hover 主色）+ 右侧 28px 圆图标（open 时橙底白字 + 箭头旋转 180°）。
- 展开态：边框换 `primary-tint` + `shadow-sm`；答案 14.5px 行高 1.85。
- 顶部关键词过滤框：420px 内、48px 高、右侧搜索图标（PRD F-SRV-03 前端过滤）。

### 7.17 政策条目（.policy-section + .policy-nav）

- 左侧锚点目录（sticky，260px 列）；右侧条目卡（编号橙色方角 + 标题 + 正文）。
- 移动端目录取消 sticky 转顶部。

### 7.18 页脚（F-GLB-04）

- 深底（`--dark`）4 列：品牌区（logo + slogan + 热线卡）/ 快捷导航 / 服务入口（**服务中心、售后政策、FAQ**）/ 关注我们（二维码 108px + 社交按钮）。
- 底部法律区：版权 / 备案号 / 公安备案 / 隐私链接，居左；右侧售后/隐私/FAQ 快捷链接。
- 响应式：≤1199px 2 列 → <768px 1 列，底部纵向堆叠。

---

## 8. 组件规范：后台管理

### 8.1 布局框架

```
┌ 侧栏 224px（深色 #1E1F26，sticky 全高） ┬ 顶栏 56px（白底，面包屑 + 帮助 + 用户） ┐
│  Logo（logo.png + JUZI PHONE/ADMIN CONSOLE）│                                        │
│  分组小标题（概览/产品管理/内容管理/客户服务/用户与权限/系统）│  内容区 padding 22/24px      │
│  菜单项（17px 图标 + 文字；active 橙底白字 + 橙色投影）│  （卡片/表格/表单）             │
│  二级子项（缩进 38px + 圆点前缀）            │                                        │
└──────────────────────────────────────────┴────────────────────────────────────────┘
```

- 侧栏：`position: sticky` 全高；激活项**橙底白字 + 0 4px 12px 橙色投影**；子菜单展开箭头旋转 90°。
- 顶栏：面包屑（`首页级/当前页`，当前页 `ink` 600）；右侧帮助图标 + 用户 chip（头像首字 + 姓名 + 角色）。
- 视图切换：`.view.active` fade + translateY 300ms。

### 8.2 Ant Design 组件映射表

| 场景 | 原型形态 | AntD 组件建议 | 定制要点 |
|---|---|---|---|
| 按钮 | btn-primary / outline / danger / sm | Button | 圆角 8px；主色 token 替换 |
| 表格 | table + table-wrap | Table | 表头浅灰 #FAFAFB；行 hover；操作列内联文字按钮 |
| 表单 | input/select/textarea/label/switch | Form + Input/Select/TextArea/Switch | focus 主色 + 3px 光晕；双语字段并排 |
| Tab | tab-btn 下划线式 | Tabs | 下划线主色；用于产品编辑 6 页签 |
| 抽屉 | drawer 480px | Drawer（right） | 留言详情 |
| 上传 | upload-box 虚线框 | Upload.Dragger | 虚线 hover 主色；类型/大小提示 |
| 日期范围 | range-picker | DatePicker.RangePicker | 图标化触发 |
| 分页 | pagination | Pagination | 橙 active |
| 面包屑 | crumb | Breadcrumb | 当前页加粗 |
| 消息反馈 | —（原型 alert 占位） | message / Modal.confirm | 成功/失败 Toast；删除二次确认 |
| 富文本 | rich-editor（工具栏占位） | 第三方（如 wangEditor/quill）+ XSS 白名单清洗 | 见 PRD B-CASE/B-NEWS |

### 8.3 表格规范

- 结构：`.card > .table-wrap > table`；表头 `#FAFAFB`、600 字重、`ink-2`；行 hover `#FAFAFC`。
- 首列常为"名称 + 缩略图"组合（`cell-name`，44px 缩略图圆角 8px）。
- 操作列：内联文字按钮（编辑 / 状态切换 / 删除），hover 变主色（删除 hover 红）。
- 多选：checkbox `accent-color: var(--primary)`；批量操作按钮在 page-head 或筛选区。
- 空态：居中 48px 图标 + 文案。

### 8.4 筛选区（.filter-bar）

- 白底卡片：搜索框（240px）、下拉筛选、日期范围、重置按钮、右侧结果计数（`共 N 条`）。
- 与列表同卡分离，位于卡片上方；表单控件高度 36px 统一。

### 8.5 表单规范

- `label` 13px/600 + 红色 `*` 必填；`hint` 12px 灰色辅助说明。
- 栅格：`cols-2` / `cols-3` 双/三列（≤640px 收单列）。
- **双语字段并排**：中英输入框并排一组（如"产品名称（中文）| 产品名称（英文）"），降低录入成本。
- switch：38×21 胶囊，on 状态橙底滑块右移。

### 8.6 产品编辑页（B-PROD-02）

- 顶部工具行：`保存草稿`（=下架态，前台不可见）+ `保存并上架`；sub 显示最后保存时间。
- 编辑卡头：产品名 + 状态 tag + `首页推荐` switch。
- 6 个 Tab（基础信息 / 版本管理 / 图集管理 / 参数规格 / 产品亮点 / 购买渠道），下划线式切换。
- 版本行（color-row）：色块（30px）+ 颜色名（含"默认版本"tag）+ HEX + 规格 + 价格 + 编辑按钮。
- 图集：`minmax(120px,1fr)` 网格，首图角标"主图"。
- 参数规格：分组卡（组名 + 项数 + 编辑分组），组内参数名/值中英并排。
- 渠道行：名称 + URL + 启用/停用 tag + 编辑。

### 8.7 状态 tag 色板

见 3.3 语义表；后台 tag 高度 24px、圆角 6px、12px/600。内置角色/删除类操作须有对应警示 tag。

### 8.8 留言详情抽屉（B-MSG-03）

- 480px 右侧抽屉（max 92vw）：详情信息格（姓名/联系方式/主题/来源/语言/时间，`info-grid` 双列灰底格）→ 留言全文（灰底圆角块）→ **状态时间线**（圆点竖线：提交 → 处理 → 回复）→ 回复输入（≤1000 字）+ 底部操作（标记处理中 / 提交回复）。
- 个人信息（手机号脱敏展示 `138****1234`）仅管理员可见（PRD B-MSG-06）。

### 8.9 权限树（B-ROLE）

- 左侧角色列表（230px：角色名 + 人数 + 内置锁图标，active 浅橙底橙字）→ 右侧权限组。
- 权限组卡：模块勾选头 + `repeat(auto-fill,minmax(170px,1fr))` 权限点勾选项（`product:view 查看` 格式）。
- 内置角色（超级管理员）不可编辑/删除（PRD B-ROLE 保护规则）。

### 8.10 统计可视化（B-DASH / B-STAT）

| 组件 | 规格 |
|---|---|
| 统计卡 | 46px 彩色图标底（浅色底 + 语义色）+ 24px/800 数字 + 标签 + 趋势（绿涨/红跌） |
| 趋势图 | 折线/面积图：PV 主色橙、UV 灰 #B8B6C0；网格线 #F0F0F2；推荐 ECharts/Recharts 复刻 |
| 页面 Top 榜 | 横条图：主色渐变条（`linear-gradient(90deg,#FF6A00,#FF9E5C)`）+ PV 数值 |
| 来源分布 | 环形/饼图：橙/浅橙/蓝/绿四色，配文字图例（禁用色盲不利组合） |

### 8.11 登录页（B-AUTH-01）

- 双栏：左侧品牌区（深棕渐变 155° + 橙色光晕，Logo + 标语 + 一句价值主张）→ 右侧居中卡片（max 400px）。
- 表单：用户名/密码（44px 高输入）+ 记住我 + 忘记密码；错误条（红底白字区，`login-error`）。
- 演示账号提示（原型开发期展示，上线前移除）；底部版权小字。

---

## 9. 页面规范：前台

> 每个页面给出结构要点、关键交互与响应式行为；与 PRD 功能编号交叉引用见附录 C。

### 9.1 首页（F-HOME-01~07）

模块自上而下：

| 模块 | 结构 | 数据/交互要点 |
|---|---|---|
| 大图轮播 | 深色渐变全宽 520px（移动 430px） | 3~5 张、5s 自动、指示点、双向箭头（移动隐藏）；每张 标题/副文案/双 CTA |
| 新品推荐 | section-head（eyebrow+标题+说明+查看全部）→ 4 列产品卡 | `is_featured=1` 最多 8 款 |
| 产品专区 | 4 张系列大卡（4:3.1，渐变底 + 底部渐变压暗 + 系列名/简介/进入） | 点击进产品列表按系列筛选 |
| 品牌故事 | 左视觉（深棕渐变 + 大号 橘子/JUZI）右文案 + 3 指标卡 + 了解更多 | 指标：500万+ 全球用户 / 120+ 研发专利 / 60+ 服务网点 |
| 应用案例 | 3 列案例卡 + 查看全部 | 精选已发布案例 ≤3 |
| 新闻动态 | 3 列新闻卡 + 查看更多 | 最新 3 条 |
| 页脚（F-HOME-07） | 见 7.18 | 服务入口含服务中心体系 |

- 区块背景交替：白 → 浅灰 → 白 → 浅灰 → 白。
- 移动端：产品卡 2 列、案例/新闻 1 列、轮播去视觉图。

### 9.2 产品中心（F-LIST）

- 面包屑：首页 / 产品 / 产品中心。
- 筛选条：系列 Tab（全部+4 系列，URL 参数 `?series=`）+ 排序下拉（最新上架/价格低→高/高→低，`?sort=`）。
- 主体：4/3/2 列产品网格 → 空态（清除筛选）→ 分页（每页 12，`?page=`）。
- 数据：仅上架产品（PRD F-LIST 列表数据规则）。

### 9.3 产品详情（F-DET）

- 面包屑：首页 / 产品 / 产品中心 / 产品名（动态）。
- 上半区：左图集（sticky）/ 右信息（名称、卖点、上市日期胶囊【可空则隐藏，PRD F-DET-02】、价格框、颜色/存储选择、购买渠道）。
- 中区：产品亮点（浅灰底、图文交错）。
- 下区：参数规格（分组表）+ 相关产品（≤4）。
- 交互：颜色选择联动主图与图片源；版本选择联动价格（起售价 vs 版本价）；渠道新窗口打开。

### 9.4 新案例展示（F-CASE）

- 列表：面包屑 → 行业分类 Tab（全部+零售/教育/制造，`?category=key`）+ 计数 → 3/2/1 列案例卡 → 空态 → 分页（每页 9）。
- 详情（原型未单独建视图，按 PRD 6.1.4）：面包屑 → 封面 → 标题/分类/时间 → 富文本正文 → 相关案例 ×3 → 返回列表。

### 9.5 新闻动态（F-NEWS）

- 列表：分类 Tab（全部/企业新闻/行业资讯，`?category=company|industry`）→ 3/2/1 列新闻卡 → 空态 → 分页（每页 9）。
- 详情：面包屑 → 封面 → 标题/时间/来源 → 富文本正文 → 返回列表。

### 9.6 招聘入口（F-CAREER）

- 统一骨架：面包屑 → Tab（社会招聘/校园招聘）→ 图文介绍区（`career-intro` 图+文案+3 福利指标）→ 招聘流程步骤条（社招 4 步/校招 5 步）→ 投递方式卡（邮箱按钮 + 二维码）。
- 交互：Tab 切换两个内容块；邮箱按钮 mailto 唤起。

### 9.7 关于我们系列（F-ABOUT）

- 关于橘子：图文布局（左文案：eyebrow+标题+两段介绍+3 价值观卡；右品牌视觉图）。
- 发展历程：单列时间线（5 个节点 2019→2026），max-width 820px。
- 品牌介绍：品牌故事段 → 视觉图 + 价值主张列表（3 条：tag 序号 + 标题 + 说明）。
- 联系我们：见 9.8。
- 页面内容由后台 `pages` 表维护（page_key：about/history/brand，PRD B-PAGE）。

### 9.8 联系我们（F-CONT）

- 左列：4 张联系信息卡（地址/电话/邮箱/时间，图标浅橙底）+ 地图占位（斜纹虚线框）。
- 右列：留言表单卡（两列栅格：姓名* / 手机 / 邮箱 / 主题 / 留言内容* 2000 字 + 字数统计 → 隐私勾选* → 提交）。
- 校验：前端即时（必填/邮箱/11 位手机号）+ 后端二次校验（PRD F-CONT-03）。
- 成功态：表单区整体切换成功视图，清空表单。

### 9.9 服务中心体系（F-SRV）

- 服务中心：图文介绍（指标：60+ 网点 / 48h 维修 / 100% 原厂配件）+ 双 CTA（联系客服 / 查看 FAQ）。
- 售后政策：左锚点目录（sticky）+ 右条目卡（编号块 + 标题 + 正文），锚点平滑滚动。
- FAQ：关键词过滤框 + 手风琴列表 + 空态（引导去联系我们）。
- 入口仅在页脚"服务入口"区（PRD 3.3.1 导航规则）。

### 9.10 站点维护页（F-GLB-05）

- 触发：后台"系统设置-其他-站点维护开关"开启后，前台**所有页面（含首页）不渲染业务内容**，统一展示维护页（PRD F-GLB-05）。
- 布局：居中纵向排列——品牌 Logo（约 108px）→ 维护提示标题（28px/800）→ 双语说明文案（`ink-2`，可含预计恢复时间，后台配置）→ 客服热线（复用页脚热线卡样式）。
- 视觉：复用品牌视觉语言（白底 + 主色点缀 + SVG 图标），**不使用 404 样式**，语义区分"临时维护"与"页面不存在"。
- 接口行为：维护开启时前台接口返回 HTTP 503 + 维护标记，前端据此统一渲染维护页；后台不受维护开关影响（管理员可登录关闭）。

### 9.11 404 与内容不存在

- 大号渐变"404"（80–140px、橙→浅橙渐变文字）→ 标题 → 说明 → 返回首页按钮。
- 内容不存在场景（产品/新闻/案例已下线）：提示"该内容已下线或不存在"+ 返回列表页入口（PRD 6.1.9）。

---

## 10. 页面规范：后台

| 页面 | 结构要点 | 关键交互 |
|---|---|---|
| 登录（view-login） | 见 8.11 | 登录成功切主布局；失败错误条 |
| 工作台（view-dashboard） | page-head（标题+时间范围）→ 5 统计卡 → 左趋势图 + 右待办留言/快捷入口 | 待办点击跳留言管理；快捷入口跳对应页 |
| 产品列表（view-products） | page-head（批量操作+新增）→ 筛选区 → 表格（多选/产品/系列/价格/日期/状态/更新/操作）→ 分页 | 上下架单条切换；删除二次确认 |
| 产品编辑（view-product-edit） | page-head（保存草稿/保存并上架）→ 编辑卡（名称+状态+首页推荐）→ 6 Tab | 保存草稿=下架；Tab 切换不丢已填 |
| 产品系列（view-series） | 表格（系列缩略色块/简介/产品数/排序/状态/操作） | 新增/编辑/删除 |
| 案例管理（view-cases） | 筛选（状态/行业）→ 表格（封面/标题/分类 key/精选/状态/时间/操作） | 编辑/下线/删除；发布走编辑页 |
| 轮播图（view-banners） | 表格（预览 76×40/标题中英/跳转类型/排序/有效期/状态） | 上线/下线；有效期提示 |
| 新闻管理（view-news） | 筛选 → 表格（标题/分类/状态/时间/操作） | 发布/下线/删除 |
| 页面内容（view-pages） | 表格（页面/page_key/前台路径/标题中英/更新时间/操作） | 6 个 page_key 仅编辑 |
| FAQ（view-faq） | 筛选 → 表格（问题/分类/排序/状态/操作） | 上下线 |
| 售后政策（view-policies） | 表格（标题中英/排序/状态/更新时间） | 上下线 |
| 留言管理（view-messages） | 筛选（状态/时间）→ 表格（姓名/脱敏联系方式/主题/摘要/来源/状态/时间/操作）→ 详情抽屉 | 状态筛选联动；抽屉内标记处理中/回复 |
| 管理员账号（view-users） | 表格（头像首字/用户名/角色/状态/最近登录/创建/操作） | 重置密码/编辑/禁用/删除（保护规则） |
| 角色管理（view-roles） | 左角色列表 + 右权限组树 | 切换角色即时加载权限；保存生效 |
| 操作日志（view-logs） | 筛选（管理员/模块/时间）→ 表格（时间/管理员/动作 tag/模块/对象/摘要/IP） | 只读 |
| 站点配置（view-configs） | 5 个配置 Tab（基础/联系方式/社交/页脚/其他） | 保存即时生效；维护开关 |
| 访问统计（view-stats） | 4 统计卡 → 趋势图 → 页面 Top 榜 + 来源分布 | 日/周/月切换 |

---

## 11. 交互与动效规范

### 11.1 时长与缓动

| 场景 | 时长 | 缓动 |
|---|---|---|
| hover 状态变化 | 150–250ms | ease |
| 视图切换（前后台） | 300–350ms | fade + translateY(8–10px) |
| 下拉/抽屉展开 | 220–280ms | cubic-bezier(.4,0,.2,1) |
| 轮播切换 | 700ms | opacity + visibility |
| 导航下划线 | 220ms | scaleX |

### 11.2 状态反馈

- **加载**：列表/图表用骨架屏或 loading 态；提交类按钮禁用并显示 loading。
- **成功**：表单提交成功切成功态视图；后台操作 message 提示。
- **失败**：内联错误就近展示（前台表单）、后台 Toast 或错误条；网络异常统一提示 + 重试。
- **空态**：友好文案 + 下一步操作（清除筛选/返回）。
- **删除类**：二次确认（后台 Modal.confirm，PRD 10.5）。

### 11.3 悬停与聚焦

- 卡片悬停上浮（-6px）+ 大阴影；按钮主色加深；链接/图标变色。
- 输入框聚焦：主色描边 + 3px 光晕；导航下拉支持 `:focus-within` 展开（键盘可达）。
- 焦点可见性：所有可交互元素保留可见焦点环（默认 outline 或自定义）。

### 11.4 滚动行为

- `scroll-behavior: smooth`；产品详情图集、售后政策目录、后台顶栏使用 sticky。
- 内容区滚动回顶：后台视图切换时 `scrollTo(0)`。

### 11.5 动效克制

- 仅使用 transform / opacity 实现动效（避免重排），动画时长 ≤ 700ms。
- 支持 `prefers-reduced-motion`：降级为无位移淡入或直接显示。

### 11.6 触控与响应

- 触控目标：主交互 ≥ 44×44px（前台按钮 46px 高满足）；42px 图标按钮（icon-btn）须满足间距隔离（与相邻可点击元素间距 ≥ 8px），间距不足时扩大至 44px。
- 移动端轮播支持手势滑动（touch 事件）；抽屉支持遮罩点击关闭。

---

## 12. 无障碍与多语言适配

### 12.1 无障碍要求

| 项 | 要求 | 落地位置 |
|---|---|---|
| 对比度 | 正文 ≥ 4.5:1；主色不作正文色（见 3.4） | 色彩 token、正文用色规则 |
| 焦点状态 | 全部交互元素可见焦点环；下拉可键盘展开 | 导航、按钮、表单、搜索 |
| 键盘导航 | Tab 顺序 = 视觉顺序；Esc 关闭抽屉/面板 | 导航、抽屉、搜索面板 |
| 语义化 | `<header><nav><main><section><footer>`；标题层级有序 | 页面骨架（PRD §10.2） |
| 图片 alt | 有意义图片配描述性 alt（产品名/机型/场景） | 全站图片 |
| 表单 label | 使用 `for` 关联；错误内联提示并 `aria-describedby` | 留言表单、搜索 |
| 纯图标按钮 | `aria-label`（搜索、汉堡、关闭、轮播箭头、社交） | 头部、轮播、页脚 |
| 状态指示 | 颜色非唯一指示（tag 必须含文字） | 前后台状态标签 |
| 触控目标 | ≥ 44px；间距防误触 | 移动端导航、按钮 |

### 12.2 多语言适配规则

- **UI 文案**：i18next 资源（`locales/zh-CN.json`、`en-US.json`），运行时切换无刷新（PRD §9.1）。
- **内容字段**：双语字段按 `?lang` 返回，英文缺失回退中文（应用层 `en || zh`）。
- **富文本**：中英正文独立存储，后台分 Tab/并排编辑。
- **文本溢出**：卡片标题两行截断；按钮/标签不换行；英文长 URL 允许断词。
- **URL 策略**：单一 URL 动态渲染；SEO 用 `hreflang` 声明（P2）。
- **数字/日期**：中文环境用中文日期格式（2026 年 9 月），英文环境 ISO/英文月份（Sep 2026）。

### 12.3 无障碍验收抽查项

- [ ] 仅用键盘可完成：登录、导航全站、表单提交、抽屉开关。
- [ ] 屏幕阅读器可读出：导航项、按钮用途（aria-label）、表单 label、错误信息。
- [ ] 放大 200% 无横向滚动、无重叠；正文无主色文字。
- [ ] 状态不依赖颜色区分（截图灰度检查）。

---

## 13. 验收清单与附录

### 13.1 交付前检查清单（视觉与交互）

**视觉质量**
- [ ] 全站 token 与本文档第 3~6 章一致（色值/字体/圆角/阴影）
- [ ] 未使用 emoji 作图标；图标统一线性 SVG 风格
- [ ] 悬停态不引起布局位移（仅 transform/opacity）
- [ ] 深浅场景文字对比达标（正文 ≥ 4.5:1）

**交互**
- [ ] 所有可点击元素 `cursor: pointer` 且有悬停反馈
- [ ] 过渡 150–300ms；`prefers-reduced-motion` 生效
- [ ] 加载/成功/失败/空态四态完整；删除有二次确认
- [ ] 键盘焦点可见；Tab 顺序正确

**响应式**
- [ ] 375 / 768 / 1024 / 1440px 四档无横向滚动、无重叠
- [ ] 断点列数与 5.4 一致；移动端导航为抽屉

**无障碍**
- [ ] 图片 alt、表单 label、纯图标 aria-label 全覆盖
- [ ] 状态标签文字 + 颜色并存

**双语**
- [ ] 中英切换即时生效；英文缺省回退中文
- [ ] 英文长文案不破版（截断/换行规则生效）

### 13.2 附录 A：设计 token 总表

**前台**

| 类别 | token | 值 |
|---|---|---|
| 主色 | primary / strong / soft / tint | #FF6A00 / #E85D00 / #FFF4EC / #FFE9D8 |
| 文字 | ink / ink-2 / ink-3 | #1A1A1A / #56534E / #9A958C |
| 边框 | line / line-strong | #E9E6E1 / #DCD8D0 |
| 背景 | bg / bg-soft | #FFFFFF / #F7F5F1 |
| 深色 | dark / dark-2 / dark-ink / dark-ink-2 | #1C1A17 / #26231F / #F2EFE9 / #A9A49B |
| 圆角 | radius / radius-lg | 14px / 20px |
| 阴影 | shadow-sm / shadow / shadow-lg | 0 2px 10px .06 / 0 10px 34px .10 / 0 22px 60px .16（rgba(28,26,23,…)） |
| 布局 | header-h / container | 72px（移动 62px）/ 1240px |
| 字体 | font | Inter, PingFang SC, HarmonyOS Sans SC, Microsoft YaHei, Noto Sans SC, sans-serif |

**后台**

| 类别 | token | 值 |
|---|---|---|
| 主色 | primary 系列 | 同前台 |
| 文字 | ink / ink-2 / ink-3 | #1C1B20 / #5A5761 / #9A97A0 |
| 边框 | line / line-strong | #E8E7EC / #DAD8DF |
| 背景 | bg / card | #F5F6F8 / #FFFFFF |
| 侧栏 | sidebar / sidebar-2 / sidebar-ink | #1E1F26 / #26272F / #9EA0AB |
| 状态 | success / danger / warn / info | #22A65E / #E5484D / #E8A30C / #3B82F6 |
| 圆角 | radius | 10px（卡 12px / 输入 8px / 按钮 8px） |

### 13.3 附录 B：设计素材清单（手机素材/）

`logo.png`（前台导航/页脚、后台侧栏/登录页）；产品渲染图按 `机型 颜色.png` 命名：JUZI 1（橘红/黑曜石/雪山白）、JUZI X1（雪山白/黑曜石/钛金灰 + X1 主图）、JUZI Flip（雪山白/黑曜石）、JUZI TriFold（橘红/雪山白）；轮播透明图 `机型 颜色 透明.png`：JUZI 1 橘红、JUZI X1 雪山白、JUZI TriFold 橘红（共 14 张）。

### 13.4 附录 C：与 PRD 功能编号交叉引用

| PRD 编号 | 对应章节 |
|---|---|
| F-GLB-01/02/03/04/05 | §7.11 导航体系（01/02/03）、§7.18 页脚（04）、§9.10 维护页与 404（05） |
| F-HOME-01~07 | §9.1 首页 |
| F-LIST-01~06 | §9.2 产品中心 |
| F-DET-01~08 | §7.13 + §9.3 产品详情 |
| F-CASE-01~09 | §9.4 案例 |
| F-NEWS-01~04 | §9.5 新闻 |
| F-CAREER-01~04 | §7.14 + §9.6 招聘 |
| F-ABOUT-01~04 | §9.7 关于系列 |
| F-CONT-01~05 | §7.10 + §9.8 联系我们 |
| F-SRV-01~03 | §7.16/7.17 + §9.9 服务中心 |
| B-AUTH-01~06 | §8.11 登录 + §8.2 映射表 |
| B-DASH-01~04 | §8.10 + §10 工作台 |
| B-PROD-01~05 / B-SERIES | §8.3~8.6 + §10 产品 |
| B-CMS（B-CASE/BANNER/NEWS/PAGE/FAQ/POLICY） | §8.2~8.4 + §10 内容管理 |
| B-MSG-01~06 | §8.8 + §10 留言管理 |
| B-USER / B-ROLE / B-LOG | §8.9 + §10 用户权限 |
| B-CONFIG / B-STAT | §8.10 + §10 系统 |

---

*本文档为橘子手机企业官网 UI/UX 设计基线，视觉改动须经评审并更新修订记录；与 PRD 冲突时以 PRD 需求为准。*
