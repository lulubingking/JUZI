# 橘子手机（JUZI PHONE）企业官网产品需求文档（PRD）

| 文档属性 | 内容 |
|---|---|
| 文档版本 | v1.2 |
| 编写日期 | 2026-08-25 |
| 文档状态 | 已评审确认（需求范围） |
| 产品定位 | 橘子手机企业官网（前台官网系统 + 后台管理系统） |
| 技术栈 | FastAPI + MySQL（后端）、React + Vite（前端） |
| 参考网站 | https://www.mi.com/index.html |

---

## 修订记录

| 版本 | 日期 | 修订人 | 修订说明 |
|---|---|---|---|
| v1.0 | 2026-08-25 | WorkBuddy | 首次定稿：需求范围经多轮确认后固化，含前台官网与后台管理全量功能定义 |
| v1.1 | 2026-08-25 | WorkBuddy | 导航结构调整：主导航收敛为 5 项（首页/产品/新闻/招聘入口/关于我们）并新增二级导航；新增"案例展示"模块（产品应用案例）；招聘改为静态招聘页；服务中心移入页脚；新闻分类调整为两类（企业新闻/行业资讯） |
| v1.2 | 2026-08-25 | WorkBuddy | 专家审核修订：①统一功能编号前缀（F-LIST/F-DET/F-CONT/F-SRV）；②修正信息架构树层级；③products 表补充 launch_date 字段（产品上市日期，与 published_at 语义区分）；④cases 表补充 category_key 稳定分类标识；⑤澄清产品"保存草稿=下架态"；⑥补充站点维护页（F-GLB-05）与 404 页面定义；⑦补充搜索联想 limit 参数；⑧首页精选案例限定已发布状态 |
| v1.3 | 2026-08-26 | WorkBuddy | 品牌与产品数据全面更新（对应前台/后台原型落地）：①品牌英文名 Orange Phone → JUZI PHONE；②系列体系重构为四大系列（旗舰/X/Flip/TriFold）；③示例产品更新为 JUZI 1 / JUZI X1 / JUZI Flip / JUZI TriFold（价格 2488/2888/3688/4888，配色/卖点同步）；④示例案例/新闻/轮播中产品名同步；⑤补充 F-DET 详情页数据来源说明（按 slug 动态渲染）；⑥补充 §12.5 设计素材资源约定；⑦后台管理需求标注已交付原型 admin.html |

---

## 目录

1. [项目概述](#1-项目概述)
2. [用户与使用场景](#2-用户与使用场景)
3. [系统总体架构](#3-系统总体架构)
4. [前台官网功能需求](#4-前台官网功能需求)
5. [后台管理系统功能需求](#5-后台管理系统功能需求)
6. [页面结构与交互说明](#6-页面结构与交互说明)
7. [数据模型设计](#7-数据模型设计)
8. [API 接口规划](#8-api-接口规划)
9. [多语言方案](#9-多语言方案)
10. [非功能需求](#10-非功能需求)
11. [里程碑与迭代规划](#11-里程碑与迭代规划)
12. [示例数据设计](#12-示例数据设计)
13. [风险与开放问题](#13-风险与开放问题)

---

## 1. 项目概述

### 1.1 项目背景

橘子手机（JUZI PHONE）是一家（虚构占位）手机制造企业，需要建设面向公众的企业官网，以承载品牌形象展示、产品信息发布、应用案例展示、新闻动态传播与用户咨询触达等核心诉求。当前企业无自有官网，品牌信息分散于第三方渠道，亟需一个统一、可控、可运营的官方门户。

### 1.2 产品目标

| 编号 | 目标 | 衡量指标 |
|---|---|---|
| G1 | 建立统一的企业品牌官方门户，展示品牌形象与产品全貌 | 官网完整覆盖品牌/产品/案例/新闻/服务五类信息 |
| G2 | 支持产品信息结构化、可运营化维护，快速上下架 | 产品从录入到上线 ≤ 30 分钟（运营后台操作） |
| G3 | 打通用户咨询链路，沉淀潜在用户线索 | 留言表单提交成功率 ≥ 99%，后台可查看、回复、跟踪 |
| G4 | 面向海内外展示，支持中英双语 | 前台界面与核心内容双语文案覆盖率 100% |
| G5 | 保障官网基础 SEO 收录与移动端访问体验 | 响应式覆盖 PC/平板/手机；关键页面支持搜索引擎索引 |

### 1.3 产品定位

- **系统形态**：纯展示型企业官网，**不包含**在线交易（无购物车、订单、支付、库存）。
- **面向对象**：
  - 前台：公众访客（潜在消费者、媒体、求职者、投资者等）。
  - 后台：企业内部运营人员（管理员、内容运营、客服）。
- **边界声明**：购买行为通过第三方渠道（京东、天猫等）承接，官网仅提供渠道跳转引导；招聘仅提供静态信息展示与投递引导，不包含在线职位投递系统。

### 1.4 参考网站

本产品以小米官网（https://www.mi.com/index.html）为信息架构与视觉层级参考，具体参考点：

| 参考维度 | 参考内容 |
|---|---|
| 信息架构 | 顶部导航分区与二级下拉导航（选购指南 / 服务中心 / 关于品牌 / 关注我们）的层级组织方式 |
| 首页结构 | 大图轮播 + 产品推荐区块 + 品牌内容区块 + 底部导航的纵向布局节奏 |
| 产品展示 | 产品卡片式陈列、详情页参数规格表的组织方式 |
| 页脚 | 服务热线、备案信息、法律声明、友情链接的规范呈现 |

> 说明：仅参考结构与层级，不复制其视觉设计、文案与素材，品牌视觉由橘子手机独立定义。

### 1.5 技术栈

| 层 | 技术选型 | 说明 |
|---|---|---|
| 后端框架 | FastAPI (Python) | 高性能异步框架，自动生成 OpenAPI 文档 |
| ORM | SQLAlchemy | 与 FastAPI 官方生态匹配，支持 Alembic 迁移 |
| 数据库 | MySQL 8.0+ | 生产级关系型数据库 |
| 认证 | JWT | 后台管理员无状态认证（access token + refresh token） |
| 图片存储 | 本地文件存储 | 开发阶段存服务器本地磁盘，预留对象存储（OSS/S3）接口扩展 |
| 前端框架 | React 18 + TypeScript | 组件化开发 |
| 构建工具 | Vite | 快速开发服务器与构建 |
| 状态管理 | Zustand / Redux Toolkit | 推荐 Zustand（轻量） |
| 路由 | React Router v6 | 前端路由 |
| 请求库 | Axios | 统一封装拦截器、错误处理 |
| UI 组件库 | Ant Design（后台）/ 自研组件（前台） | 后台表格/表单效率优先；前台定制视觉 |
| 多语言 | i18next + react-i18next | 中英双语 |

### 1.6 术语表

| 术语 | 含义 |
|---|---|
| 前台官网系统 | 面向公众访问的展示站点（Frontend Public Site） |
| 后台管理系统 | 面向内部运营的管理控制台（Admin Console） |
| 产品版本（Variant） | 同一产品下按颜色、存储容量组合的 SKU 维度信息 |
| 产品参数（Spec） | 产品的技术规格属性（如屏幕、电池、摄像头） |
| 应用案例（Case） | 产品在真实场景中的落地应用案例（图文内容） |
| 留言（Message） | 访客通过联系我们等表单提交的咨询/反馈内容 |
| 轮播图（Banner） | 首页顶部大图轮播位，支持配置跳转 |

---

## 2. 用户与使用场景

### 2.1 用户画像

#### 2.1.1 前台访客

| 画像 | 描述 | 核心诉求 |
|---|---|---|
| 潜在消费者 | 关注手机产品，比较参数与价格 | 快速了解产品系列、价格、购买渠道 |
| 企业/政企客户 | 关注产品实际落地效果 | 查看应用案例，评估产品适用性 |
| 品牌关注者 | 关注企业动态、新品发布 | 获取最新新闻、行业资讯 |
| 求职者 | 了解企业文化与招聘信息 | 访问招聘入口（社会招聘/校园招聘） |
| 媒体/合作方 | 了解企业背景与联系渠道 | 获取联系方式、企业资料 |

#### 2.1.2 后台运营角色

| 角色 | 职责 | 使用模块 |
|---|---|---|
| 超级管理员 | 系统全量管理、账号与权限分配、站点配置 | 全部模块 |
| 内容运营 | 产品/案例上下架与内容维护、新闻发布、轮播图与 FAQ 维护 | 产品管理、内容管理 |
| 客服 | 处理访客留言咨询、维护 FAQ | 留言管理、内容管理(FAQ) |

### 2.2 核心使用场景

| 编号 | 场景 | 角色 | 流程概述 |
|---|---|---|---|
| S1 | 访客浏览官网 | 访客 | 打开首页 → 查看轮播与新品 → 进入产品详情 → 查看参数与购买渠道 |
| S2 | 访客查看案例 | 访客 | 产品 → 新案例展示 → 浏览案例列表 → 查看案例详情 |
| S3 | 求职者查看招聘 | 求职者 | 导航"招聘入口" → 选择社会/校园招聘 → 查看流程与投递方式 |
| S4 | 访客提交咨询 | 访客 | 关于我们 → 联系我们 → 填写留言表单 → 提交成功提示 |
| S5 | 运营维护产品/案例 | 内容运营 | 登录后台 → 产品管理/案例管理 → 新增/编辑内容 → 上架发布 |
| S6 | 客服处理留言 | 客服 | 登录后台 → 留言管理 → 查看待处理留言 → 回复/标记完成 |
| S7 | 管理员分配权限 | 超级管理员 | 登录后台 → 用户与权限 → 新建管理员账号/分配角色 |

---

## 3. 系统总体架构

### 3.1 系统组成

```
┌─────────────────────────────────────────────────────────┐
│                    橘子手机企业官网                        │
├───────────────────────────────┬─────────────────────────┤
│  前台官网系统 (Public Site)     │  后台管理系统 (Admin)    │
│  React + Vite (SPA)           │  React + Vite (SPA)     │
│  - 首页 / 产品 / 案例 / 新闻    │  - 登录认证              │
│  - 招聘 / 关于 / 联系 / 服务    │  - 产品/内容/留言/权限    │
│  - 中英双语 / 响应式 / SEO      │  - 统计 / 日志 / 配置     │
└───────────────┬───────────────┴────────────┬────────────┘
                │  RESTful API (HTTPS)       │  RESTful API + JWT
┌───────────────▼────────────────────────────▼────────────┐
│                 FastAPI 后端服务 (Python)                  │
│  业务模块：产品/案例/内容/留言/用户/权限/统计/日志/上传/配置    │
│  中间件：JWT 鉴权 / 请求日志 / CORS / 限流 / 异常处理         │
├──────────────────────────────────────────────────────────┤
│  SQLAlchemy ORM + Alembic 迁移           本地文件存储/对象存储│
├──────────────────────────────────────────────────────────┤
│                  MySQL 8.0 数据库                          │
└──────────────────────────────────────────────────────────┘
```

### 3.2 技术架构说明

1. **前后端分离**：前台官网与后台管理均为独立 React SPA，通过 RESTful API 与 FastAPI 交互，可独立构建部署。
2. **接口划分**：公开接口（`/api/public/*`，无需鉴权）与管理接口（`/api/admin/*`，需 JWT 鉴权）严格分离。
3. **SEO 策略**：SPA 对搜索引擎不友好，采用 **SSR/预渲染** 策略——推荐使用 `vite-plugin-ssr` 或构建期静态预渲染关键页面（首页、产品列表、产品详情、案例列表、新闻列表、新闻详情），配合 `sitemap.xml` 与 meta 标签管理（详见 10.2）。
4. **图片资源**：统一走 `/api/admin/upload` 上传，返回可访问 URL；图片加载采用懒加载与 WebP 格式转换（如部署环境支持）。
5. **配置管理**：站点级配置（热线、地址、版权等）存 MySQL `site_configs` 表，后台可编辑，前台通过公开接口读取。

### 3.3 信息架构

#### 3.3.1 前台信息架构

```
橘子手机官网
├── 首页 (/)                                [一级导航 · 无二级]
├── 产品 (/products)                        [一级导航 · 下拉二级]
│   ├── 产品中心 (/products)                  ← 二级①
│   └── 新案例展示 (/cases)
│       └── 案例详情 (/cases/:id)
├── 新闻 (/news)                            [一级导航 · 下拉二级]
│   ├── 企业新闻 (/news?category=company)     ← 二级①
│   └── 行业资讯 (/news?category=industry)    ← 二级②
│       └── 新闻详情 (/news/:id)
├── 招聘入口 (/careers/social)               [一级导航 · 下拉二级]
│   ├── 社会招聘 (/careers/social)            ← 二级①
│   └── 校园招聘 (/careers/campus)            ← 二级②
├── 关于我们 (/about)                        [一级导航 · 下拉二级]
│   ├── 关于橘子 (/about)                     ← 二级①
│   ├── 发展历程 (/about/history)             ← 二级②
│   ├── 品牌介绍 (/about/brand)               ← 二级③
│   └── 联系我们 (/contact)                   ← 二级④（含留言表单）
└── 页脚服务区（非顶部导航）                    [售后政策 / 常见问题 / 服务中心]
    ├── 服务中心 (/service)
    ├── 售后政策 (/service/policy)
    └── 常见问题 FAQ (/service/faq)
```

**导航交互规则**：
- 顶部一级导航共 5 项：首页、产品、新闻、招聘入口、关于我们。
- 带二级导航的一级项：PC 端悬停展开下拉，移动端点击展开子菜单；一级项文字本身可点击，无落地页的一级项默认跳转其第一个二级页面（"产品"→ /products、"新闻"→ /news、"招聘入口"→ /careers/social、"关于我们"→ /about）。
- 服务中心体系（服务中心 / 售后政策 / FAQ）**不进入顶部导航**，入口位于页脚"服务入口"区。

#### 3.3.2 后台信息架构

```
后台管理系统 (登录后)
├── 工作台（仪表盘：统计概览、快捷入口、待办留言）
├── 产品管理
│   ├── 产品列表（筛选/搜索/上下架）
│   ├── 产品编辑（基础信息/版本/图集/参数/亮点/购买渠道）
│   └── 产品系列管理
├── 内容管理
│   ├── 案例管理（应用案例列表/编辑）
│   ├── 轮播图管理
│   ├── 新闻管理
│   ├── 页面内容（关于橘子/发展历程/品牌介绍/服务中心/社会招聘/校园招聘）
│   ├── 常见问题 FAQ
│   └── 售后政策
├── 留言管理（列表/详情/回复/删除）
├── 用户与权限
│   ├── 管理员账号
│   ├── 角色管理（角色-权限配置）
│   └── 操作日志
├── 系统设置
│   ├── 站点配置（联系方式/版权/备案/社交）
│   └── 访问统计
└── 个人中心（修改密码）
```

---

## 4. 前台官网功能需求

> 功能需求编号规则：`F-页面-编号`（如 F-HOME-01）。优先级：P0（必须有）/ P1（重要）/ P2（可选）。

### 4.1 全局功能

#### 4.1.1 顶部导航（F-GLB-01，P0）

| 项 | 说明 |
|---|---|
| 内容 | Logo（点击回首页）+ 一级导航（5 项）+ 语言切换 + 搜索入口 + 移动端汉堡菜单 |
| 一级导航 | 首页、产品、新闻、招聘入口、关于我们（**共 5 项，固定顺序**） |
| 二级导航 | 产品▾：产品中心、新案例展示；新闻▾：企业新闻、行业资讯；招聘入口▾：社会招聘、校园招聘；关于我们▾：关于橘子、发展历程、品牌介绍、联系我们 |
| 交互 | 页面滚动超过视口后导航吸顶（sticky），背景半透明加深；PC 悬停展开下拉，当前路由对应菜单高亮 |
| 响应式 | ≤ 768px 时折叠为抽屉式菜单，一级项可展开二级列表 |

#### 4.1.2 语言切换（F-GLB-02，P0）

- 提供"中文 / English"切换开关，位于顶部导航右侧。
- 切换后：界面文案（导航、按钮、表单标签）即时切换；内容字段（产品名、新闻标题等）取对应语言版本。
- 用户选择持久化至 `localStorage`，默认值取浏览器语言（`zh` 优先中文，其余默认英文）。
- 无对应语言内容时回退展示默认语言（中文）并允许配置。

#### 4.1.3 站内搜索（F-GLB-03，P1）

- 搜索范围：产品名称/卖点、应用案例标题/摘要、新闻标题/摘要。
- 交互：导航栏搜索框输入 → 下拉联想（最多 8 条）→ 回车进入搜索结果页。
- 结果页：分产品 / 案例 / 新闻三组展示，支持分页。

#### 4.1.4 页脚（F-GLB-04，P0）

| 区块 | 内容 |
|---|---|
| 品牌区 | Logo、品牌 Slogan、客服热线（配置）、服务时间 |
| 快捷导航 | 产品中心 / 新案例展示 / 新闻动态 / 招聘入口 / 关于我们 / 联系我们 |
| 服务入口 | **服务中心、售后政策、常见问题**（服务中心体系入口，位于此处） |
| 关注我们 | 官方微博、微信公众号（二维码图）、社交链接（可配置） |
| 法律区 | 备案号、版权信息、隐私声明链接（均由后台配置） |

#### 4.1.5 站点维护页（F-GLB-05，P1）

- 触发条件：后台"系统设置-其他-站点维护开关"开启后，前台**所有页面**（含首页）显示维护页，不渲染业务内容。
- 维护页内容：品牌 Logo、维护提示文案（双语）、预计恢复时间（可选，后台配置）、客服热线。
- 后台不受维护开关影响，管理员可正常登录关闭维护开关；前台接口在维护开启时返回维护状态标记（HTTP 503 + 提示），便于前端统一渲染维护页。

### 4.2 首页（F-HOME，P0）

| 编号 | 模块 | 需求说明 |
|---|---|---|
| F-HOME-01 | 大图轮播 | 顶部全宽轮播（3~5 张，自动播放 5s/张，支持手势滑动与指示点）；每张含标题、副标题、跳转链接（内部页面/外链）；内容由后台"轮播图管理"配置 |
| F-HOME-02 | 新品推荐 | 展示后台标记"首页推荐"的产品（最多 8 款），卡片式横排：产品图、名称、价格（起售价）、查看详情入口；点击进入产品详情 |
| F-HOME-03 | 产品专区 | 按产品系列分区展示（如"旗舰系列 / X 系列 / Flip 系列 / TriFold 系列"），大图 + 系列名 + 简介 + 跳转产品列表（按系列筛选） |
| F-HOME-04 | 品牌故事 | 品牌理念图文区块（后台"页面内容-关于橘子"节选或独立配置），提供"了解更多"跳转 /about |
| F-HOME-05 | 应用案例 | 精选应用案例卡片（最多 3 条：标题、摘要、封面），"查看全部"跳转 /cases |
| F-HOME-06 | 新闻动态 | 最新 3 条新闻卡片（标题、摘要、日期），"查看更多"跳转 /news |
| F-HOME-07 | 底部导航+页脚 | 见 4.1.4 |

**首页数据来源优先级**：轮播图（banners）→ 新品推荐（products.is_featured=1）→ 产品系列（series）→ 精选案例（cases.is_featured=1 **且已发布**）→ 新闻最新 3 条（news）→ 站点配置（site_configs）。

### 4.3 产品中心（F-LIST，P0）

> 产品中心即产品列表页，路径 /products，为"产品"一级导航下的二级页面①。

| 编号 | 功能 | 需求说明 |
|---|---|---|
| F-LIST-01 | 产品陈列 | 卡片网格（PC 4 列 / 平板 3 列 / 手机 2 列），卡片含产品图、名称（当前语言）、一句话卖点、起售价 |
| F-LIST-02 | 系列筛选 | 顶部 Tab 按产品系列筛选（全部 + 各系列），与 URL 参数联动（`?series=xxx`）便于分享 |
| F-LIST-03 | 排序 | 支持：最新上架 / 价格从低到高 / 价格从高到低 |
| F-LIST-04 | 分页 | 每页 12 条，页码与 URL 参数联动（`?page=2`） |
| F-LIST-05 | 空态 | 无结果时展示友好空态提示与"清除筛选"按钮 |
| F-LIST-06 | SEO | 页面标题/描述 meta 随筛选动态更新，支持预渲染 |

**列表数据**：仅展示 `status=上架` 的产品；下架产品前台不可见。

### 4.4 产品详情页（F-DET，P0）

| 编号 | 功能 | 需求说明 |
|---|---|---|
| F-DET-01 | 产品图集 | 左侧主图 + 缩略图列表（点击切换），支持放大预览；图为后台配置的多图（`product_images`） |
| F-DET-02 | 基本信息 | 产品名称（当前语言）、一句话卖点、起售价、上市日期（取 `products.launch_date`，可空则隐藏） |
| F-DET-03 | 版本选择 | 颜色（色块 + 名称）与存储容量组合选择，选中后联动显示对应版本价格；颜色显示配置的色值 |
| F-DET-04 | 购买渠道 | 渠道按钮列表（如"京东自营 / 天猫旗舰店 / 官方商城"），点击新窗口打开渠道链接（后台可配置） |
| F-DET-05 | 参数规格 | 分组的参数规格表（如"基本参数 / 屏幕 / 性能 / 影像 / 电池"），字段名与值均支持双语 |
| F-DET-06 | 产品亮点 | 核心卖点图文区块（标题 + 描述 + 配图），自上而下排列 |
| F-DET-07 | 相关产品 | 同系列其他产品推荐（最多 4 款） |
| F-DET-08 | SEO | 独立 meta title/description，支持预渲染；结构化数据（Schema.org Product）可选 |

**版本价格展示规则**：展示"起售价"（最低版本价格）；选中具体版本后展示该版本价格；如仅有一个版本，直接展示价格。

**数据来源**：详情页全部展示字段（产品名称/卖点/配色/图集/价格/参数规格/亮点）均取自该产品数据，前台按产品标识（slug）动态渲染，避免多产品共享同一静态详情模板。

### 4.5 新案例展示（F-CASE，P0）

> 应用案例展示，路径 /cases，为"产品"一级导航下的二级页面②。

#### 4.5.1 案例列表页（F-CASE-LIST）

| 编号 | 功能 | 需求说明 |
|---|---|---|
| F-CASE-01 | 案例陈列 | 卡片网格（PC 3 列 / 平板 2 列 / 手机 1 列），卡片含封面图、标题、摘要、所属行业分类、发布时间 |
| F-CASE-02 | 行业分类筛选 | 顶部 Tab 按行业分类筛选（全部 + 各分类），与 URL 参数联动（`?category=<分类key>`，如 `retail`），分类 key 为稳定英文标识 |
| F-CASE-03 | 分页 | 每页 9 条，页码与 URL 参数联动 |
| F-CASE-04 | 空态 | 无结果时展示友好空态提示 |
| F-CASE-05 | SEO | 页面 meta 动态更新，支持预渲染 |

**列表数据**：仅展示 `is_published=1` 且 `published_at <= 当前时间` 的案例。

#### 4.5.2 案例详情页（F-CASE-DETAIL）

| 编号 | 功能 | 需求说明 |
|---|---|---|
| F-CASE-06 | 内容展示 | 面包屑 → 封面图 → 标题 / 分类 / 发布时间 → 富文本正文（图文混排） |
| F-CASE-07 | 相关案例 | 同分类其他案例推荐（最多 3 条） |
| F-CASE-08 | 返回入口 | 返回案例列表按钮 |
| F-CASE-09 | SEO | 独立 meta title/description，支持预渲染 |

### 4.6 新闻动态（F-NEWS，P0）

| 编号 | 功能 | 需求说明 |
|---|---|---|
| F-NEWS-01 | 新闻列表 | 卡片列表（封面图、标题、摘要、发布日期），按发布时间倒序，分页每页 9 条；路径 /news |
| F-NEWS-02 | 分类 | **两类**：企业新闻（company）、行业资讯（industry）；分类与二级导航一一对应；二级导航"企业新闻"直达 `/news?category=company`，"行业资讯"直达 `/news?category=industry`；列表页含分类 Tab 切换 |
| F-NEWS-03 | 新闻详情 | 富文本正文（支持图片、标题层级），展示标题、发布时间、来源；底部"返回列表" |
| F-NEWS-04 | SEO | 新闻详情 meta 动态生成，支持预渲染 |

**发布规则**：仅展示 `is_published=1` 且 `published_at <= 当前时间` 的新闻。

### 4.7 招聘入口（F-CAREER，P0）

> 静态招聘页。一级导航"招聘入口"点击默认进入社会招聘页（/careers/social），下拉含社会招聘、校园招聘两个二级页面。

| 编号 | 功能 | 需求说明 |
|---|---|---|
| F-CAREER-01 | 社会招聘页 | 路径 /careers/social：招聘理念与福利介绍（图文）、社会招聘流程（如"简历投递 → 简历筛选 → 面试 → Offer"步骤条）、投递方式（招聘邮箱、二维码等，后台可配置） |
| F-CAREER-02 | 校园招聘页 | 路径 /careers/campus：校园招聘介绍（图文）、校招流程（如"网申 → 宣讲 → 笔试 → 面试 → Offer"步骤条）、投递方式（招聘邮箱、二维码等） |
| F-CAREER-03 | 内容维护 | 两个页面内容均由后台"内容管理-页面内容"维护（page_key：career_social / career_campus），支持双语富文本 |
| F-CAREER-04 | 边界声明 | 不提供在线职位列表与在线投递系统（如后续需要，见开放问题 O-02） |

### 4.8 关于我们（F-ABOUT，P0）

> 关于我们为一级导航，下拉含 4 个二级页面。

| 编号 | 页面 | 路径 | 需求说明 |
|---|---|---|---|
| F-ABOUT-01 | 关于橘子 | /about | 企业简介、品牌理念、企业文化图文内容；由后台"页面内容（page_key=about）"维护 |
| F-ABOUT-02 | 发展历程 | /about/history | 企业发展历程时间线（年份 + 事件，图文）；由后台"页面内容（page_key=history）"维护（运营在富文本中排版时间线） |
| F-ABOUT-03 | 品牌介绍 | /about/brand | 品牌定位、品牌标识（LOGO/VI）、品牌价值主张；由后台"页面内容（page_key=brand）"维护 |
| F-ABOUT-04 | 联系我们 | /contact | 联系信息展示 + 留言/咨询表单（详见 4.9） |

**页面模板**：四个二级页面统一使用"页面内容"模板（横幅图 → 标题 → 富文本正文），联系我们为独立模板（含表单）。

### 4.9 联系我们（F-CONT，P0）

> 关于我们一级导航下的二级页面④，路径 /contact。

| 编号 | 功能 | 需求说明 |
|---|---|---|
| F-CONT-01 | 联系信息展示 | 公司地址、电话、邮箱、工作时间、地图占位（后台配置） |
| F-CONT-02 | 留言/咨询表单 | 字段：姓名（必填）、手机号/邮箱（二选一必填）、主题（选填）、留言内容（必填，≤ 2000 字）、来源页面（隐藏字段）；提交成功后展示成功提示并清空表单 |
| F-CONT-03 | 表单校验 | 前端即时校验（必填、邮箱格式、手机号格式 11 位）；后端二次校验 |
| F-CONT-04 | 防垃圾 | 提交接口加入 Honeypot 隐藏字段 + 接口限流（同 IP 每分钟 ≤ 3 次），防止机器人刷单 |
| F-CONT-05 | 隐私提示 | 表单下方展示"提交即表示同意隐私声明"勾选项（必勾选才可提交） |

**留言状态流转**：`待处理 → 处理中 → 已回复`（后台操作，详见 5.5）。

### 4.10 服务中心体系（F-SRV，P0）

> 服务中心体系**不进入顶部导航**，入口位于页脚"服务入口"区，含三个页面。

| 编号 | 页面 | 路径 | 需求说明 |
|---|---|---|---|
| F-SRV-01 | 服务中心 | /service | 服务理念与服务体系说明（服务承诺、服务网点覆盖等图文）、客服热线（后台配置）、"联系客服"入口；由后台"页面内容（page_key=service）"维护 |
| F-SRV-02 | 售后政策 | /service/policy | 售后政策条目（退换货政策、保修政策、维修流程等），条目由后台"售后政策"管理维护，支持双语与排序；页面按条目分组展示（目录 + 正文），支持锚点定位 |
| F-SRV-03 | 常见问题 FAQ | /service/faq | 按分类分组的问答折叠列表（手风琴交互），支持关键词过滤（前端过滤）；内容由后台"FAQ 管理"维护，支持双语、排序、上下线；空态提示"未找到相关问题，请通过联系我们咨询" |

---

## 5. 后台管理系统功能需求

> 需求编号规则：`B-模块-编号`。所有后台页面要求：响应式布局（侧边栏可折叠）、统一列表组件（筛选/排序/分页/批量操作）、统一表单组件（校验/上传/双语字段）、操作成功/失败 Toast 反馈。

> 后台已交付高保真原型 `admin.html`（含登录、工作台、产品/内容/留言/用户权限/系统设置全部模块），作为前端实现参考；下表功能编号与原型视图一一对应。

### 5.1 登录与认证（B-AUTH，P0）

| 编号 | 功能 | 需求说明 |
|---|---|---|
| B-AUTH-01 | 登录页 | 账号（用户名）+ 密码登录，支持"记住我"（延长 token 有效期） |
| B-AUTH-02 | 认证机制 | JWT：access token（2h）+ refresh token（7d）；access token 过期后自动刷新 |
| B-AUTH-03 | 安全策略 | 密码 BCrypt 加密存储；连续 5 次密码错误锁定账号 30 分钟；登录记录操作日志 |
| B-AUTH-04 | 修改密码 | 个人中心支持修改密码（需验证旧密码）；密码复杂度：≥ 8 位，含字母+数字 |
| B-AUTH-05 | 路由守卫 | 前端路由守卫：未登录跳转登录页；无权限页面显示 403 提示 |
| B-AUTH-06 | 会话失效 | 401 统一拦截，刷新 token 失败后清除本地会话并跳转登录页 |

### 5.2 工作台（B-DASH，P0）

| 编号 | 功能 | 需求说明 |
|---|---|---|
| B-DASH-01 | 统计卡片 | 产品总数（含上架数）、案例总数（含已发布数）、新闻总数、待处理留言数、本月访问量（PV/UV） |
| B-DASH-02 | 待办提醒 | 待处理留言列表（最近 5 条），点击跳转留言管理 |
| B-DASH-03 | 快捷入口 | 常用操作：新增产品、发布新闻、新增案例、查看留言 |
| B-DASH-04 | 访问趋势 | 近 30 日 PV/UV 折线图（前端图表库：ECharts / Recharts） |

### 5.3 产品管理（B-PROD，P0）

#### 5.3.1 产品列表

| 编号 | 功能 | 需求说明 |
|---|---|---|
| B-PROD-01 | 列表展示 | 表格：封面缩略图、名称（中）、系列、价格（起售价）、上架状态、更新时间、操作 |
| B-PROD-02 | 筛选搜索 | 按状态（全部/上架/下架）、系列筛选；按名称关键字搜索 |
| B-PROD-03 | 上下架 | 单条切换上架/下架；下架产品前台立即不可见 |
| B-PROD-04 | 批量操作 | 批量上架/下架/删除（删除为逻辑删除，见 7.2） |
| B-PROD-05 | 删除保护 | 产品存在版本/图集等子数据时删除需二次确认，提示子数据一并删除 |

#### 5.3.2 产品编辑（Tab 页签式）

| 页签 | 字段/功能 |
|---|---|
| 基础信息 | 名称（中/英）、系列、一句话卖点（中/英）、产品简介（中/英）、起售价、上市日期（可选）、封面图（单图上传+裁剪）、上架状态、首页推荐开关、SEO title/description（中/英） |
| 版本管理 | 版本列表（表格）：颜色名称（中/英）、色值（HEX）、存储容量（如 256GB）、运行内存、价格、默认版本标记；支持新增/编辑/删除/排序 |
| 图集管理 | 图片列表（网格缩略图）：主图标记、排序、删除；上传支持多图、拖拽排序、预览 |
| 参数规格 | 分组维护：分组名（中/英）+ 参数项（名称中/英 + 值中/英）；支持分组新增/编辑/排序 |
| 产品亮点 | 亮点条目（标题中/英、描述中/英、配图、排序） |
| 购买渠道 | 渠道条目（渠道名中/英、URL、排序、启停用） |

**表单规则**：
- 双语字段：中英文案均为必填（P0），英文缺失时保存给出警告但允许保存草稿，前台回退中文。
- 起售价自动取"版本价格最低值"，如无版本则手动填写。
- 保存时机：整个编辑页支持"保存草稿 / 保存并上架"；**"保存草稿"即保存为下架状态（status=0），前台不可见，与"上架"（status=1）语义对齐（产品不设独立草稿态）**。

#### 5.3.3 产品系列管理（B-SERIES，P1）

- 系列 CRUD：名称（中/英）、简介（中/英）、排序、状态。
- 产品可归属于一个系列（可选，不强制，无系列产品归入"全部"）。

### 5.4 内容管理（B-CMS，P0）

#### 5.4.1 案例管理（B-CASE，P0）

| 功能 | 说明 |
|---|---|
| 列表 | 封面缩略图、标题（中）、行业分类、发布状态、发布时间、操作（编辑/发布/下线/删除） |
| 筛选 | 按状态（全部/已发布/草稿）、行业分类筛选；按标题关键字搜索 |
| 编辑 | 标题（中/英）、摘要（中/英）、正文（中/英，富文本编辑器：支持标题、段落、图片、列表、引用）、封面图、行业分类（分类 key 为稳定英文标识，如 `retail`；名称中/英，可维护分类项）、首页精选开关、SEO title/description（中/英） |
| 发布 | 支持"保存草稿"与"发布"；发布记录发布时间；已发布可"下线"（前台隐藏） |
| 富文本安全 | 富文本内容做 XSS 清洗（白名单标签/属性），禁止上传脚本 |

#### 5.4.2 轮播图管理（B-BANNER）

| 功能 | 说明 |
|---|---|
| 列表 | 缩略图、标题（中/英）、跳转类型（内部页面/外链/无）、排序、状态（上线/下线）、有效期 |
| 编辑 | 标题（中/英）、副标题（中/英）、图片上传（建议 16:7 比例，提示尺寸）、跳转配置、排序、上线/下线、有效期（起止时间，可空=长期） |
| 规则 | 仅"上线"且"当前时间在有效期内"的轮播图在首页展示；超出有效期自动不展示 |

#### 5.4.3 新闻管理（B-NEWS）

| 功能 | 说明 |
|---|---|
| 列表 | 标题（中）、分类、发布状态、发布时间、操作（编辑/发布/下线/删除） |
| 编辑 | 标题（中/英）、摘要（中/英）、分类（**企业新闻 / 行业资讯** 二选一）、封面图、正文（中/英，富文本编辑器）、SEO title/description |
| 发布 | 支持"保存草稿"与"发布"；发布时记录发布时间；已发布可"下线"（前台隐藏） |
| 富文本安全 | 富文本内容做 XSS 清洗（白名单标签/属性），禁止上传脚本 |

#### 5.4.4 页面内容管理（B-PAGE）

- 管理以下页面内容（每个 page_key 仅一条记录，后台修改即时同步前台）：

| page_key | 对应前台页面 | 路径 |
|---|---|---|
| about | 关于橘子 | /about |
| history | 发展历程 | /about/history |
| brand | 品牌介绍 | /about/brand |
| service | 服务中心 | /service |
| career_social | 社会招聘 | /careers/social |
| career_campus | 校园招聘 | /careers/campus |

- 编辑字段：标题（中/英）+ 富文本正文（中/英）+ 封面图（可选）。

#### 5.4.5 FAQ 管理（B-FAQ）

- 列表：问题（中）、分类、状态、排序。
- 编辑：分类（可维护，如"购买咨询/售后/使用问题"）、问题（中/英）、答案（中/英）、排序、上线/下线。
- 前台仅展示"上线"的 FAQ。

#### 5.4.6 售后政策管理（B-POLICY）

- 条目列表：标题（中/英）、排序、状态。
- 编辑：标题（中/英）、正文（中/英，富文本）、排序、上线/下线。

### 5.5 留言管理（B-MESSAGE，P0）

| 编号 | 功能 | 需求说明 |
|---|---|---|
| B-MSG-01 | 列表 | 表格：姓名、联系方式、主题、内容摘要、来源页面、语言、状态、提交时间、操作 |
| B-MSG-02 | 筛选 | 按状态（全部/待处理/处理中/已回复）、时间范围筛选；按姓名/内容关键字搜索 |
| B-MSG-03 | 详情与回复 | 查看完整留言，填写回复内容（≤ 1000 字）提交；回复后状态变"已回复"，前台暂不展示回复（内部处理，见 O-03） |
| B-MSG-04 | 状态流转 | 待处理 →（标记）→ 处理中 →（回复）→ 已回复；支持撤回重开 |
| B-MSG-05 | 删除 | 单条删除（逻辑删除），二次确认 |
| B-MSG-06 | 数据保护 | 联系方式等个人信息仅管理员可见；导出（CSV）可选（P2） |

### 5.6 用户与权限（B-AUTH-MGMT，P0）

#### 5.6.1 管理员账号管理（B-USER）

| 功能 | 说明 |
|---|---|
| 列表 | 用户名、姓名、角色、状态（启用/禁用）、最近登录时间、创建时间 |
| 新增/编辑 | 用户名（唯一）、姓名、手机/邮箱、角色（单选）、初始密码、状态 |
| 禁用/启用 | 禁用后立即无法登录，已登录会话下一次请求失效（鉴权时校验状态） |
| 删除 | 不允许删除当前登录账号与最后一个超级管理员 |
| 重置密码 | 超级管理员可重置指定账号密码 |

#### 5.6.2 角色管理（B-ROLE）

| 功能 | 说明 |
|---|---|
| 角色列表 | 预置角色：超级管理员（全部权限，不可删）、内容运营、客服；支持自定义角色 |
| 权限配置 | 按权限点勾选分配（权限点清单见下表），保存后即时生效 |
| 保护规则 | 超级管理员角色不可编辑/删除 |

**权限点清单（RBAC）**：

| 模块 | 权限点 code |
|---|---|
| 产品 | `product:view` `product:create` `product:update` `product:delete` `product:publish` |
| 内容 | `cms:case` `cms:banner` `cms:news` `cms:page` `cms:faq` `cms:policy` |
| 留言 | `message:view` `message:reply` `message:delete` |
| 用户权限 | `user:view` `user:manage` `role:manage` |
| 系统 | `config:view` `config:update` `stat:view` `log:view` |

**默认角色权限矩阵**：

| 权限点 | 超级管理员 | 内容运营 | 客服 |
|---|---|---|---|
| 产品全部 | ✅ | ✅ | — |
| cms:case / banner / news / page / policy | ✅ | ✅ | — |
| cms:faq | ✅ | ✅ | ✅ |
| message 全部 | ✅ | — | ✅ |
| user / role / config / stat / log | ✅ | —（stat 可看） | — |

#### 5.6.3 操作日志（B-LOG，P1）

- 记录：管理员、操作动作（新增/编辑/删除/上下架/发布/登录等）、模块、目标对象、详情摘要、IP、时间。
- 列表支持按管理员、模块、时间范围筛选；日志只读不可删改；保留最近 90 天。

### 5.7 系统设置（B-CONFIG，P0）

| 配置项 | 说明 |
|---|---|
| 站点基础 | 站点名称（中/英）、Logo、Slogan（中/英） |
| 联系方式 | 客服热线、服务时间、公司地址（中/英）、邮箱、地图坐标（占位） |
| 社交关注 | 微博链接、微信公众号二维码图、其他社交链接（名称+URL，可增删） |
| 页脚法律 | 版权文案（中/英）、ICP 备案号、公安备案号、隐私声明链接 |
| 其他 | 每页列表条数、站点维护开关（开启后前台显示维护页） |

- 保存规则：分组 Tab 编辑，保存即时生效；修改记录操作日志。

### 5.8 访问统计（B-STAT，P1）

- 埋点方式：前台页面埋点脚本上报 PV/UV（按 URL + 日期聚合，UV 以 IP + User-Agent 去重，P2 可升级为访客 ID）。
- 展示：工作台趋势图 + "访问统计"页（按日/周/月维度、按页面 Top 榜）。
- 数据保留：保留最近 180 天。

---

## 6. 页面结构与交互说明

### 6.1 前台页面结构

#### 6.1.1 首页

```
┌────────────────────────────────────────────────┐
│ [Logo] 首页 产品▾ 新闻▾ 招聘入口▾ 关于我们▾ │ 中/EN | 搜索 │ ← 吸顶导航
├────────────────────────────────────────────────┤
│             大图轮播 Banner（3~5 张，自动+手势）        │
├────────────────────────────────────────────────┤
│ 新品推荐：产品卡片 × 4（图 / 名称 / 起售价 / 查看详情）   │
├────────────────────────────────────────────────┤
│ 产品专区：系列大图卡 × 3（系列名 / 简介 / 进入产品页）    │
├────────────────────────────────────────────────┤
│ 品牌故事：图文区（标题 / 文案 / 了解更多 → /about）      │
├────────────────────────────────────────────────┤
│ 应用案例：案例卡片 × 3（封面 / 标题 / 摘要 / 查看全部）   │
├────────────────────────────────────────────────┤
│ 新闻动态：新闻卡片 × 3（标题 / 摘要 / 日期 / 查看更多）   │
├────────────────────────────────────────────────┤
│ 页脚：品牌区 / 快捷导航 / 服务入口(服务中心/政策/FAQ) / 关注我们 / 法律区 │
└────────────────────────────────────────────────┘
```

#### 6.1.2 产品中心（产品列表页）

```
┌────────────────────────────────────────────────┐
│ [导航栏（同上）]                                    │
│ 面包屑：首页 / 产品 / 产品中心                      │
│ 系列 Tab：[全部 | 旗舰系列 | X系列 | Flip系列 | TriFold系列]  │
│ 排序：[最新上架 ▾]                                    │
│ ┌────┐ ┌────┐ ┌────┐ ┌────┐                       │
│ │卡片│ │卡片│ │卡片│ │卡片│  ← 4 列网格（产品图/名称/卖点/价格）│
│ └────┘ └────┘ └────┘ └────┘                       │
│ [上一页] 1 2 3 [下一页]                              │
│ [页脚]                                             │
└────────────────────────────────────────────────┘
```

#### 6.1.3 产品详情页

```
┌────────────────────────────────────────────────┐
│ [导航栏] 面包屑：首页 / 产品 / 产品中心 / 产品名         │
│ ┌──────────────┐ ┌────────────────────────────┐ │
│ │ 主图          │ │ 产品名（当前语言）               │ │
│ │ 缩略图×n       │ │ 一句话卖点                    │ │
│ │              │ │ 起售价 / 版本选择（颜色+存储）     │ │
│ └──────────────┘ │ 购买渠道：[京东] [天猫] [官网]    │ │
│                  │ 参数规格速览（前 4 项 + 展开全部）  │ │
│                  └────────────────────────────┘ │
│ 产品亮点区块（标题 / 描述 / 大图，自上而下）             │
│ 参数规格表（分组表格，字段名+值双语）                   │
│ 相关产品 × 4                                        │
│ [页脚]                                             │
└────────────────────────────────────────────────┘
```

#### 6.1.4 新案例展示页

**列表页（/cases）**：

```
┌────────────────────────────────────────────────┐
│ [导航栏] 面包屑：首页 / 产品 / 新案例展示              │
│ 行业分类 Tab：[全部 | 零售 | 教育 | 制造 | ...]        │
│ ┌──────────┐ ┌──────────┐ ┌──────────┐           │
│ │ 案例卡片   │ │ 案例卡片   │ │ 案例卡片   │ ← 3 列网格  │
│ │(封面/标题/ │ │          │ │          │  (封面/标题/  │
│ │ 摘要/分类) │ │          │ │          │   摘要/分类) │
│ └──────────┘ └──────────┘ └──────────┘           │
│ [上一页] 1 2 [下一页]                                │
│ [页脚]                                             │
└────────────────────────────────────────────────┘
```

**详情页（/cases/:id）**：面包屑 → 封面图 → 标题/分类/发布时间 → 富文本正文 → 相关案例 × 3 → 返回列表。

#### 6.1.5 新闻列表与详情

- **列表（/news）**：分类 Tab（全部 / 企业新闻 / 行业资讯）→ 卡片列表（封面/标题/摘要/日期）→ 分页。
- **详情（/news/:id）**：面包屑 → 封面图 → 标题/时间/来源 → 富文本正文 → 返回列表。

#### 6.1.6 关于我们系列页面

- **统一模板**（关于橘子 / 发展历程 / 品牌介绍）：横幅图 → 标题 → 富文本正文（图文段落）。
- **联系我们（/contact）**：左侧联系信息卡片（地址/电话/邮箱/时间）+ 右侧留言表单；提交成功展示成功态。

#### 6.1.7 招聘页面

- **统一模板**（社会招聘 / 校园招聘）：横幅图 → 标题 → 招聘理念图文区 → 招聘流程步骤条（3~5 步）→ 投递方式区（邮箱/二维码）。

#### 6.1.8 服务中心体系页面

- **服务中心（/service）**：横幅图 → 服务理念图文 → 服务承诺/网点 → 客服热线与联系入口。
- **售后政策（/service/policy）**：条目目录（锚点）+ 条目正文，自上而下排列。
- **FAQ（/service/faq）**：左侧分类锚点（可选）+ 右侧问答折叠列表 + 顶部关键词过滤框。

#### 6.1.9 404 页面

- 路由未匹配或内容不存在（如产品/新闻/案例已下线）时展示 404 页：品牌 Logo、提示文案（当前语言）、"返回首页"按钮。
- 内容不存在时页面可展示"该内容已下线或不存在"，并提供返回列表页入口（如适用）。

### 6.2 后台页面结构

#### 6.2.1 布局（通用）

```
┌──────┬─────────────────────────────────────────┐
│ Logo │ [面包屑]                    [用户头像▾]     │
│ ──── │ ─────────────────────────────────────── │
│ 工作台 │  筛选区（搜索框 / 下拉筛选 / 时间范围）         │
│ 产品管理▸│  ┌─────────────────────────────────┐  │
│   ├产品列表│  │ 表格 / 卡片列表（分页、排序、操作列）    │  │
│   └系列管理│  └─────────────────────────────────┘  │
│ 内容管理▸│  分页器          [新增按钮 / 批量操作]       │
│   ├案例管理│                                        │
│   ├轮播图 │                                        │
│   ├新闻管理│                                        │
│   ├页面内容│                                        │
│   ├FAQ    │                                        │
│   └售后政策│                                        │
│ 留言管理 │                                        │
│ 用户权限▸│                                        │
│ 系统设置 │                                        │
│ 访问统计 │                                        │
├──────┴─────────────────────────────────────────┤
│ © 2026 JUZI PHONE Admin                      │
└────────────────────────────────────────────────┘
```

#### 6.2.2 关键页面说明

| 页面 | 结构 |
|---|---|
| 登录页 | 居中卡片：Logo、账号、密码、"记住我"、登录按钮、错误提示 |
| 产品编辑 | 顶部：名称 + 保存按钮组（保存草稿/保存并上架）；主体：Tab 页签（基础信息/版本/图集/参数/亮点/渠道） |
| 案例编辑 | 顶部：标题 + 保存按钮组；主体：表单（双语字段分 Tab 编辑 + 封面图上传 + 分类选择 + 富文本编辑器） |
| 留言详情 | 抽屉或新页：留言全文 + 联系信息 + 状态时间线 + 回复表单 |
| 角色权限 | 左侧角色列表 + 右侧权限树（按模块分组勾选） |
| 站点配置 | 分组 Tab：基础/联系方式/社交/页脚/其他 |

---

## 7. 数据模型设计

### 7.1 ER 关系总览

```
admin_users 1──n roles（role_id）
roles 1──n role_permissions n──1 permissions
products 1──n product_variants / product_images / product_specs / product_highlights / purchase_channels
cases（独立，含 category 字段）
banners（独立）
news（独立，含 category 字段）
pages（page_key 唯一：about/history/brand/service/career_social/career_campus）
faqs（独立）
service_policies（独立）
messages 1──0..1 admin_users（replied_by）
site_configs（key-value JSON）
operation_logs 1──0..1 admin_users
visit_stats（独立）
```

### 7.2 通用约定

- 主键：`id BIGINT UNSIGNED AUTO_INCREMENT`。
- 时间字段：`created_at`、`updated_at`（DATETIME，默认 CURRENT_TIMESTAMP，更新自动刷新）。
- 逻辑删除：核心业务表（products、cases、news、messages、faqs 等）含 `deleted_at DATETIME NULL`，删除置为当前时间，查询默认过滤。
- 字符集：`utf8mb4`（支持中文+emoji）；排序规则 `utf8mb4_unicode_ci`。
- 双语字段：命名 `xxx_zh` / `xxx_en`，`NOT NULL`，前台按语言取用，缺英文回退中文（应用层处理）。
- 金额：`DECIMAL(10,2)`，单位：人民币元。

### 7.3 数据表定义

#### 表 admin_users（后台管理员）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | |
| username | VARCHAR(50) UNIQUE NOT NULL | 登录账号 |
| password_hash | VARCHAR(255) NOT NULL | BCrypt 哈希 |
| name | VARCHAR(50) | 姓名 |
| email | VARCHAR(100) | 邮箱 |
| phone | VARCHAR(20) | 手机号 |
| avatar | VARCHAR(255) | 头像 URL |
| role_id | BIGINT FK→roles | 角色 |
| status | TINYINT | 1 启用 / 0 禁用 |
| failed_attempts | INT DEFAULT 0 | 连续失败次数 |
| locked_until | DATETIME NULL | 锁定截止时间 |
| last_login_at | DATETIME NULL | 最近登录时间 |
| created_at / updated_at | DATETIME | |

#### 表 roles（角色）与 permissions（权限）、role_permissions（关联）

| roles | permissions | role_permissions |
|---|---|---|
| id | id | id |
| name VARCHAR(50) | name VARCHAR(50) | role_id FK |
| code VARCHAR(50) UNIQUE | code VARCHAR(50) UNIQUE（如 `cms:case`） | permission_id FK |
| description VARCHAR(200) | module VARCHAR(50)（分组） | |
| is_builtin TINYINT（1=内置不可删） | description VARCHAR(200) | |

#### 表 products（产品）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | |
| name_zh / name_en | VARCHAR(100) NOT NULL | 名称 |
| slug | VARCHAR(120) UNIQUE NOT NULL | 详情页 URL 标识（自动生成，可改） |
| series_id | BIGINT FK→product_series NULL | 所属系列（可空） |
| tagline_zh / tagline_en | VARCHAR(200) | 一句话卖点 |
| description_zh / description_en | TEXT | 产品简介 |
| price | DECIMAL(10,2) | 起售价（默认取最低版本价） |
| launch_date | DATE NULL | 产品上市日期（前台详情页展示用，区别于 published_at 运营上架时间） |
| cover_image | VARCHAR(255) | 封面图 |
| status | TINYINT | 1 上架 / 0 下架 |
| is_featured | TINYINT | 1 首页推荐 |
| seo_title_zh / seo_title_en | VARCHAR(150) | SEO 标题 |
| seo_desc_zh / seo_desc_en | VARCHAR(300) | SEO 描述 |
| published_at | DATETIME NULL | 上架时间 |
| sort_order | INT | 排序权重（越小越前） |
| deleted_at | DATETIME NULL | 逻辑删除 |

#### 表 product_series（产品系列）

| 字段 | 说明 |
|---|---|
| id / name_zh / name_en / description_zh / description_en | |
| slug VARCHAR(120) UNIQUE | 筛选参数 |
| sort_order / status / deleted_at | |

#### 表 product_variants（产品版本：颜色×存储）

| 字段 | 说明 |
|---|---|
| id / product_id FK | |
| color_zh / color_en VARCHAR(50) | 颜色名 |
| color_hex VARCHAR(9) | 色值（如 `#FF6A00`） |
| storage VARCHAR(20) | 存储容量（如 `256GB`） |
| ram VARCHAR(20) | 运行内存（如 `12GB`） |
| price DECIMAL(10,2) | 该版本价格（由运营在后台维护；原型演示按"基础价 + 增额"生成） |
| is_default TINYINT | 默认版本 |
| sort_order / deleted_at | |

#### 表 product_images（产品图集）

| 字段 | 说明 |
|---|---|
| id / product_id FK | |
| image_url VARCHAR(255) | |
| is_main TINYINT | 主图标记 |
| sort_order | |

#### 表 product_specs（参数规格）

| 字段 | 说明 |
|---|---|
| id / product_id FK | |
| group_zh / group_en VARCHAR(50) | 分组名（如"屏幕"） |
| name_zh / name_en VARCHAR(100) | 参数名 |
| value_zh / value_en VARCHAR(255) | 参数值 |
| sort_order | |

#### 表 product_highlights（产品亮点）

| 字段 | 说明 |
|---|---|
| id / product_id FK | |
| title_zh / title_en VARCHAR(100) | |
| description_zh / description_en TEXT | |
| image_url VARCHAR(255) | |
| sort_order | |

#### 表 purchase_channels（购买渠道）

| 字段 | 说明 |
|---|---|
| id / product_id FK | |
| name_zh / name_en VARCHAR(50) | 渠道名 |
| url VARCHAR(500) | 渠道链接 |
| status TINYINT / sort_order | |

#### 表 cases（应用案例）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | |
| title_zh / title_en | VARCHAR(200) NOT NULL | 案例标题 |
| summary_zh / summary_en | VARCHAR(500) | 摘要 |
| content_zh / content_en | LONGTEXT | 富文本正文 |
| cover_image | VARCHAR(255) | 封面图 |
| category_key | VARCHAR(50) NOT NULL | 行业分类稳定标识（如 `retail`/`education`/`manufacturing`），用于前台 URL 筛选参数与接口查询 |
| category_zh / category_en | VARCHAR(50) | 行业分类展示名（如"零售"/"Retail"） |
| is_featured | TINYINT | 1 首页精选 |
| status | TINYINT | 0 草稿 / 1 已发布 / 2 已下线 |
| published_at | DATETIME NULL | 发布时间 |
| seo_title_zh / seo_title_en | VARCHAR(150) | SEO 标题 |
| seo_desc_zh / seo_desc_en | VARCHAR(300) | SEO 描述 |
| sort_order | INT | 排序权重 |
| created_at / updated_at | DATETIME | |
| deleted_at | DATETIME NULL | 逻辑删除 |

> 说明：行业分类采用"key（稳定英文标识）+ 展示名（中/英）"两字段，运营在编辑时录入维护（无需独立分类表，P2 可升级为分类字典表）；前台筛选与接口查询一律使用 `category_key`。

#### 表 banners（轮播图）

| 字段 | 说明 |
|---|---|
| id | |
| title_zh / title_en VARCHAR(100) | |
| subtitle_zh / subtitle_en VARCHAR(200) | 副标题 |
| image_url VARCHAR(255) | |
| link_type TINYINT | 0 无 / 1 内部页面 / 2 外链 |
| link_url VARCHAR(500) | 跳转地址 |
| sort_order / status TINYINT | 1 上线 / 0 下线 |
| start_at DATETIME NULL / end_at DATETIME NULL | 有效期 |

#### 表 news（新闻）

| 字段 | 说明 |
|---|---|
| id | |
| title_zh / title_en VARCHAR(200) | |
| summary_zh / summary_en VARCHAR(500) | 摘要 |
| content_zh / content_en LONGTEXT | 富文本正文 |
| cover_image VARCHAR(255) | |
| category VARCHAR(20) | 分类：company（企业新闻）/ industry（行业资讯） |
| status TINYINT | 0 草稿 / 1 已发布 / 2 已下线 |
| published_at DATETIME NULL | 发布时间 |
| seo_title_zh / seo_title_en / seo_desc_zh / seo_desc_en | |
| deleted_at | |

#### 表 pages（页面内容）

| 字段 | 说明 |
|---|---|
| id | |
| page_key VARCHAR(50) UNIQUE | **about / history / brand / service / career_social / career_campus** |
| title_zh / title_en VARCHAR(200) | |
| content_zh / content_en LONGTEXT | 富文本 |
| cover_image VARCHAR(255) | |
| updated_at | |

#### 表 faqs（常见问题）

| 字段 | 说明 |
|---|---|
| id | |
| category_zh / category_en VARCHAR(50) | 分类 |
| question_zh / question_en VARCHAR(300) | |
| answer_zh / answer_en TEXT | |
| sort_order / status / deleted_at | |

#### 表 service_policies（售后政策条目）

| 字段 | 说明 |
|---|---|
| id | |
| title_zh / title_en VARCHAR(200) | |
| content_zh / content_en LONGTEXT | |
| sort_order / status / deleted_at | |

#### 表 messages（留言）

| 字段 | 说明 |
|---|---|
| id | |
| name VARCHAR(50) NOT NULL | 姓名 |
| phone VARCHAR(20) NULL | 手机号（与邮箱至少一个） |
| email VARCHAR(100) NULL | |
| subject VARCHAR(100) NULL | 主题 |
| content TEXT NOT NULL | 留言内容 |
| source_page VARCHAR(100) | 来源页面（埋点） |
| locale VARCHAR(10) | 提交时语言 |
| status TINYINT | 0 待处理 / 1 处理中 / 2 已回复 |
| reply_content TEXT NULL | 回复内容 |
| replied_by BIGINT FK→admin_users NULL | 回复人 |
| replied_at DATETIME NULL | 回复时间 |
| deleted_at | |

#### 表 site_configs（站点配置）

| 字段 | 说明 |
|---|---|
| id | |
| config_key VARCHAR(50) UNIQUE | 如 `contact`、`social`、`footer`、`basic` |
| config_value JSON | 配置内容（分组 JSON） |
| description VARCHAR(200) | |
| updated_at | |

#### 表 operation_logs（操作日志）

| 字段 | 说明 |
|---|---|
| id | |
| admin_id FK NULL | |
| action VARCHAR(50) | login/create/update/delete/publish... |
| module VARCHAR(50) | product/case/news/message... |
| target_id VARCHAR(50) | 目标对象 id |
| detail VARCHAR(500) | 摘要 |
| ip VARCHAR(50) | |
| created_at | |

#### 表 visit_stats（访问统计）

| 字段 | 说明 |
|---|---|
| id | |
| page_url VARCHAR(255) | |
| page_name VARCHAR(100) | |
| visit_date DATE | |
| pv INT | |
| uv INT | |
| UNIQUE(page_url, visit_date) | 按日去重累计 |

---

## 8. API 接口规划

> 统一规范：RESTful JSON；成功 `{ "code": 0, "data": ..., "message": "ok" }`；失败 `{ "code": 非0, "message": "错误说明" }`；分页参数 `page`（默认1）、`page_size`（默认12，上限100）；返回分页结构 `{ "items": [], "total": n, "page": p, "page_size": s }`；时间格式 ISO 8601；管理接口请求头 `Authorization: Bearer <token>`。

### 8.1 前台公开接口（`/api/public/*`，无需鉴权，支持多语言参数 `?lang=zh|en`）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/public/banners` | 首页轮播（仅上线且在有效期） |
| GET | `/api/public/series` | 产品系列列表 |
| GET | `/api/public/products` | 产品列表：`series`、`sort`(newest/price_asc/price_desc)、`page`、`page_size`；仅返回上架产品 |
| GET | `/api/public/products/{slug}` | 产品详情（含版本/图集/参数/亮点/渠道） |
| GET | `/api/public/products/{slug}/related` | 同系列相关产品 |
| GET | `/api/public/cases` | 案例列表：`category`（分类 key，如 retail/education/manufacturing）、`page`、`page_size`；仅已发布 |
| GET | `/api/public/cases/{id}` | 案例详情 |
| GET | `/api/public/cases/{id}/related` | 同分类相关案例 |
| GET | `/api/public/news` | 新闻列表：`category`(company/industry)、`page`；仅已发布 |
| GET | `/api/public/news/{id}` | 新闻详情 |
| GET | `/api/public/pages/{page_key}` | 页面内容（about/history/brand/service/career_social/career_campus） |
| GET | `/api/public/faqs` | FAQ 列表（含分类） |
| GET | `/api/public/policies` | 售后政策列表 |
| GET | `/api/public/site-configs` | 站点配置（脱敏后：不返回内部项） |
| GET | `/api/public/search` | 搜索：`keyword`、`type`(product/case/news/all)、`page`、`page_size`；导航联想复用本接口并传 `limit`（如 8）取前 N 条 |
| POST | `/api/public/messages` | 提交留言（含 Honeypot 字段 `website` 与限流） |
| GET | `/sitemap.xml` | SEO 站点地图（静态生成） |

### 8.2 后台管理接口（`/api/admin/*`，JWT 鉴权 + RBAC 校验）

| 模块 | 接口 |
|---|---|
| 认证 | `POST /api/admin/auth/login` · `POST /api/admin/auth/refresh` · `GET /api/admin/auth/me` · `PUT /api/admin/auth/password` |
| 工作台 | `GET /api/admin/dashboard/overview` · `GET /api/admin/dashboard/trend?days=30` |
| 产品 | `GET/POST /api/admin/products` · `GET/PUT/DELETE /api/admin/products/{id}` · `PATCH /api/admin/products/{id}/status`（上下架） · `PATCH /api/admin/products/{id}/featured` |
| 产品子资源 | `POST/PUT/DELETE /api/admin/products/{id}/variants[/{vid}]`（图集/参数/亮点/渠道同构：images、specs、highlights、channels） |
| 系列 | `GET/POST /api/admin/series` · `PUT/DELETE /api/admin/series/{id}` |
| 案例 | `GET/POST /api/admin/cases` · `GET/PUT/DELETE /api/admin/cases/{id}` · `PATCH /api/admin/cases/{id}/publish` · `PATCH .../unpublish` · `PATCH .../featured` |
| 轮播图 | `GET/POST /api/admin/banners` · `PUT/DELETE /api/admin/banners/{id}` · `PATCH .../status` |
| 新闻 | `GET/POST /api/admin/news` · `GET/PUT/DELETE /api/admin/news/{id}` · `PATCH /api/admin/news/{id}/publish` · `PATCH .../unpublish` |
| 页面内容 | `GET /api/admin/pages` · `PUT /api/admin/pages/{page_key}` |
| FAQ | `GET/POST /api/admin/faqs` · `PUT/DELETE /api/admin/faqs/{id}` · `PATCH .../status` |
| 售后政策 | `GET/POST /api/admin/policies` · `PUT/DELETE /api/admin/policies/{id}` |
| 留言 | `GET /api/admin/messages`（筛选） · `GET /api/admin/messages/{id}` · `POST /api/admin/messages/{id}/reply` · `PATCH /api/admin/messages/{id}/status` · `DELETE /api/admin/messages/{id}` |
| 管理员 | `GET/POST /api/admin/users` · `PUT/DELETE /api/admin/users/{id}` · `PATCH .../status` · `POST .../reset-password` |
| 角色 | `GET/POST /api/admin/roles` · `GET /api/admin/roles/{id}/permissions` · `PUT /api/admin/roles/{id}/permissions` · `DELETE /api/admin/roles/{id}` |
| 权限点 | `GET /api/admin/permissions` |
| 日志 | `GET /api/admin/logs`（筛选：admin/module/time） |
| 统计 | `GET /api/admin/stats/overview` · `GET /api/admin/stats/visits?dimension=day|week|month` · `GET /api/admin/stats/pages`（Top 页面） |
| 配置 | `GET /api/admin/configs` · `PUT /api/admin/configs/{key}` |
| 上传 | `POST /api/admin/upload`（multipart 图片；限制类型 jpg/png/webp，≤ 5MB；返回 URL） |

### 8.3 接口鉴权与限流

- 管理接口：统一 JWT 校验中间件 + 权限点校验（装饰器按 `module:action` 匹配权限点）。
- 公开接口：留言提交接口限流（IP 维度：3 次/分钟）；全站公开接口基础限流（60 次/分钟/IP）。
- CORS：仅允许配置的前台/后台域名访问。

---

## 9. 多语言方案

### 9.1 架构设计

| 层 | 方案 |
|---|---|
| 界面文案（UI 文案） | i18next 资源文件：`locales/zh-CN.json`、`locales/en-US.json`；前端运行时切换，无刷新 |
| 业务内容 | 数据库双语字段（`xxx_zh` / `xxx_en`），API 按 `?lang` 返回对应字段（英文缺失回退中文） |
| 富文本内容 | 双语正文独立存储（content_zh / content_en），编辑端提供双 Tab 分别编辑 |
| URL 策略 | 不区分语言路径（单一 URL），内容随语言切换动态渲染；SEO 通过 `<link rel="alternate" hreflang>` 声明双语言（P2） |

### 9.2 语言回退规则

1. 用户选择语言（localStorage）→ 2. 浏览器语言（`zh*` 取中文，否则英文）→ 3. 默认中文。
2. 内容缺失英文：应用层自动回退展示中文（`en || zh`）。
3. 后台编辑校验：中文必填、英文允许暂缺（保存草稿）；正式上架要求双语完整（给出提示，不强制拦截）。

### 9.3 需要双语的资产清单

导航、按钮、表单标签、提示文案、错误信息（UI 文案）；产品名称/卖点/简介/版本颜色/参数/亮点/渠道名、**案例标题/摘要/正文/行业分类**、新闻标题/摘要/正文、页面内容（关于橘子/发展历程/品牌介绍/服务中心/招聘页）、FAQ、售后政策、站点配置（名称/地址/Slogan/版权）（业务内容）。

---

## 10. 非功能需求

### 10.1 响应式适配

| 断点 | 设备 | 布局要点 |
|---|---|---|
| ≥ 1200px | 桌面 | 产品网格 4 列、案例网格 3 列；导航全量展示（含二级下拉） |
| 768px ~ 1199px | 平板 | 产品网格 3 列、案例网格 2 列；导航收起次级项 |
| < 768px | 手机 | 产品网格 2 列、案例网格 1 列；抽屉式导航（一级可展开二级）；表单单列；轮播手势滑动 |

- 验收：主流机型（iPhone 13/14/15、主流 Android 1080p）与主流浏览器（Chrome/Edge/Safari/Firefox 近 2 个版本）无横向滚动、无元素重叠。

### 10.2 SEO 优化

| 项 | 要求 |
|---|---|
| 预渲染/SSR | 关键页面（首页、产品列表、产品详情、案例列表、案例详情、新闻列表、新闻详情）采用构建期静态预渲染或 SSR，保证爬虫可见核心内容 |
| meta 管理 | 每页动态 title/description/keywords（前后台均可维护产品、案例与新闻的 SEO 字段） |
| 语义化 | 使用 `<header><nav><main><section><footer>` 等语义标签；图片含 `alt` |
| sitemap | 生成 `sitemap.xml`（产品详情、案例详情、新闻详情、主要页面），随内容发布更新 |
| robots.txt | 配置允许抓取，屏蔽后台与内部路径 |
| 规范 | 规范 URL、面包屑导航；可选结构化数据（Product / NewsArticle / Article） |

### 10.3 性能要求

| 指标 | 目标 |
|---|---|
| 首屏加载（关键页面） | 移动端 4G 网络 ≤ 3s；桌面光纤 ≤ 2s |
| 页面交互响应 | 前端路由切换 ≤ 300ms 可交互 |
| API 响应 | 公开列表/详情接口 P95 ≤ 300ms（本地环境基准） |
| 图片 | 懒加载 + 尺寸自适应（srcset）+ WebP 优先；首屏轮播图预加载 |
| 缓存 | 静态资源强缓存（hash 文件名）；公开接口按内容实现 ETag/缓存头 |
| 体积 | 首屏 JS/CSS 按路由分包（Code Splitting），首屏 Bundle 可分析优化 |

### 10.4 安全要求

| 项 | 要求 |
|---|---|
| 认证安全 | 密码 BCrypt；JWT 签名密钥环境变量管理；token 刷新机制防重放（refresh token 轮换） |
| 注入防护 | SQLAlchemy 参数化查询；富文本 XSS 清洗（白名单）；输出转义防 XSS |
| 传输安全 | 生产环境强制 HTTPS；安全响应头（HSTS、X-Content-Type-Options、CSP） |
| 接口安全 | 管理接口 RBAC 鉴权；上传接口校验文件类型/大小/内容（魔术字节）；公开接口限流防刷 |
| 数据安全 | 留言等个人信息最小化收集；管理接口日志记录；数据库备份策略（每日全量+binlog） |
| 其他 | 登录失败锁定；验证码（P2，留言高频时启用）；后台地址不暴露于 robots/sitemap |

### 10.5 可用性与兼容性

- 关键操作（登录、保存、发布）均有加载/成功/失败反馈；删除类操作二次确认。
- 表单错误信息具体化（指出字段与原因）；网络异常统一错误提示与重试。
- 浏览器兼容：Chrome / Edge / Safari / Firefox 近 2 个大版本；不支持 IE。

---

## 11. 里程碑与迭代规划

> 以"2 人（1 前端 + 1 后端）"为参考编制；每阶段产出可演示增量。

| 里程碑 | 周期 | 范围 | 验收标准 |
|---|---|---|---|
| M1 基础工程 | 第 1 周 | 前后端脚手架、数据库初始化与迁移、后台登录认证（JWT+RBAC 骨架）、后台布局框架、上传服务 | 可登录后台、可上传图片、权限骨架生效 |
| M2 产品模块 | 第 2 周 | 产品系列、产品 CRUD（含版本/图集/参数/亮点/渠道子资源）、产品中心与产品详情前台页面（单语言） | 后台可完整维护一款产品，前台可浏览产品详情 |
| M3 内容模块 | 第 3 周 | 案例管理、轮播图、新闻、页面内容、FAQ、售后政策管理；首页组装（轮播+新品+案例+新闻） | 后台内容可维护，首页各区块数据联动正确 |
| M4 前台完善 | 第 4 周 | 全部前台页面（案例、新闻、关于系列、招聘页、服务系列、联系我们）、留言表单、中英双语、响应式 | 前台全页面可用、双语切换生效、移动端适配达标 |
| M5 系统能力 | 第 5 周 | 留言管理闭环、用户与角色权限完善、操作日志、访问统计、站点配置 | 客服处理留言闭环、权限矩阵生效、统计图表正确 |
| M6 优化上线 | 第 6 周 | SEO（预渲染/sitemap）、性能优化、安全加固、示例数据灌入、全量联调测试 | 通过 SEO/性能/安全验收清单，演示数据齐备，可部署上线 |

---

## 12. 示例数据设计

> 用于开发完成后的演示与验收。以下数据在"示例数据初始化脚本"中提供（seed 脚本），可一键灌入并可清空。

### 12.1 示例产品（4 款）

| 产品 | 系列 | 起售价 | 版本示例 | 一句话卖点 |
|---|---|---|---|---|
| JUZI 1 | 旗舰系列 | ¥2,488 | 橘红/黑曜石/雪山白 × 12GB+256GB / 16GB+512GB / 16GB+1TB | 旗舰影像，重构想象 |
| JUZI X1 | X 系列 | ¥2,888 | 雪山白/黑曜石/钛金灰 × 8GB+256GB / 12GB+512GB | 全能进阶，实力派新选 |
| JUZI Flip | Flip 系列 | ¥3,688 | 雪山白/黑曜石 × 12GB+256GB / 16GB+512GB | 折叠新形态，轻盈掌心间 |
| JUZI TriFold | TriFold 系列 | ¥4,888 | 橘红/雪山白 × 12GB+256GB / 16GB+512GB | 三折大屏，开合自成天地 |

**产品参数示例（JUZI 1）**：

| 分组 | 参数（中/英） | 值（中/英） |
|---|---|---|
| 基本参数 | 上市时间 | 2026 年 9 月 / Sep 2026 |
| 屏幕 | 尺寸 | 6.7 英寸 / 6.7 inch |
| 屏幕 | 刷新率 | 120Hz |
| 性能 | 处理器 | 第二代橘芯 X2 / JUZI Core X2 Gen 2 |
| 影像 | 主摄 | 5000 万像素 1 英寸大底 / 50MP 1-inch sensor |
| 电池 | 容量 | 5200mAh |

**亮点示例**：超感光影像系统（三主摄全焦段）；120Hz 臻彩屏；橘芯 X2 双芯快充（120W）。

**购买渠道示例**：京东自营旗舰店（https://item.jd.com/...）、天猫官方旗舰店（https://...tmall.com/...）、官网商城占位链接。

### 12.2 示例应用案例（3 条）

| 标题 | 分类 key | 行业分类 | 摘要 |
|---|---|---|---|
| JUZI 1 助力连锁零售数字化巡检 | retail | 零售 | 为全国 2,000+ 门店提供统一巡检终端，效率提升 60% |
| 智慧课堂移动终端项目：3,000 台 JUZI Flip 落地校园 | education | 教育 | 教学互动终端规模化部署，覆盖 60 所中小学 |
| 制造车间防爆作业移动方案 | manufacturing | 制造 | 通过安全认证的专用终端，支撑产线全流程数字化 |

### 12.3 示例新闻（3 条）

| 标题 | 分类 | 摘要 |
|---|---|---|
| 橘子手机发布 JUZI 1，旗舰影像再进化 | 企业新闻 | 全新 1 英寸大底主摄与橘芯 X2，开启移动影像新篇章 |
| 橘子手机 2026 秋季新品发布会定档 9 月 | 企业新闻 | 发布会将于 9 月 15 日举行，届时多款新品亮相 |
| 全球智能手机市场回暖，中端机型竞争加剧 | 行业资讯 | 行业数据显示中端价位段份额持续扩大，厂商加速产品迭代 |

### 12.4 示例其他内容

| 类型 | 示例 |
|---|---|
| 轮播图 | 3 张：JUZI 1 首发（跳产品详情）、新品发布（跳新闻）、品牌理念（跳关于橘子） |
| FAQ | 4 条：售后保修多久？/ 如何查询服务网点？/ 支持哪些渠道购买？/ 是否支持以旧换新？ |
| 售后政策 | 3 条：七天无理由退货 / 一年质保 / 维修服务流程 |
| 页面内容 | 关于橘子（品牌简介）、发展历程（时间线：成立→首款产品→旗舰系列发布）、品牌介绍（品牌定位与 VI 说明）、服务中心（服务理念）、社会招聘/校园招聘（招聘流程与投递邮箱占位） |
| 站点配置 | 客服热线 400-000-0000、服务时间 8:00-18:00、地址（占位）、备案号（占位） |
| 后台账号 | 超级管理员 `admin`（初始化密码，首次登录强制改密）；内容运营 `operator`；客服 `support` |

### 12.5 设计素材资源

> 设计素材存放于项目 `手机素材/` 目录，供开发期演示与上线运营使用（前台/后台原型均已引用）。

| 素材 | 文件命名 | 说明 |
|---|---|---|
| 品牌 Logo | `logo.png` | 透明底橘子图形，用于前台导航/页脚与后台侧栏/登录页 |
| 产品渲染图 | `机型 颜色.png`（如 `JUZI 1 橘红.png`） | 产品列表/详情/相关产品图片，按机型与配色命名 |
| 轮播透明图 | `机型 颜色 透明.png`（如 `JUZI 1 橘红 透明.png`） | 透明底产品图，用于首页轮播大图等深色背景场景 |

**当前素材清单**：`logo.png`；JUZI 1（橘红/黑曜石/雪山白）；JUZI X1（雪山白/黑曜石/钛金灰）；JUZI Flip（雪山白/黑曜石）；JUZI TriFold（橘红/雪山白）；轮播透明图 3 张（JUZI 1 橘红 / JUZI X1 雪山白 / JUZI TriFold 橘红）。

---

## 13. 风险与开放问题

### 13.1 风险清单

| 编号 | 风险 | 影响 | 应对 |
|---|---|---|---|
| R-01 | SPA 对 SEO 不友好，影响收录 | 高 | 关键页面预渲染/SSR（M6 落地）；meta 后台可维护 |
| R-02 | 双语内容维护成本翻倍 | 中 | 英文允许暂缺回退中文；后台按语言 Tab 编辑，字段级校验提示 |
| R-03 | 图片素材（产品图/轮播图/案例图）依赖运营供给 | 中 | 提供上传尺寸/比例规范提示；占位图方案用于开发期演示 |
| R-04 | 留言接口被垃圾刷单 | 中 | Honeypot + 限流 + 后台状态筛选（已列入 P0） |
| R-05 | 权限配置不当导致越权 | 高 | RBAC 统一鉴权装饰器；默认角色矩阵固化；上线前权限走查 |

### 13.2 开放问题（待后续决策）

| 编号 | 问题 | 建议 |
|---|---|---|
| O-01 | 是否需要前台在线客服（IM）接入？ | 本期不做，留言表单已覆盖；如需可对接第三方客服工具 |
| O-02 | 招聘页是否需要升级为在线职位列表与投递？ | 本期为静态招聘页（v1.1 已确认）；如需职位模块二期评估 |
| O-03 | 留言回复是否对访客可见（邮件通知/页面查询）？ | 本期内部处理；如需访客可见回复，需增加查询凭证机制（二期） |
| O-04 | 是否接入第三方统计（如百度统计/Google Analytics）？ | 本期自研埋点；后续可叠加第三方脚本 |
| O-05 | 部署环境与域名是否已确定？ | 待定，影响 HTTPS 证书、CDN、对象存储配置 |
| O-06 | 产品"购买渠道"跳转外链与案例内容的法律合规（广告法）？ | 上线前请法务复核文案与外链 |

---

*本文档为橘子手机企业官网开发的需求基线，后续变更须经需求评审并更新修订记录。*
